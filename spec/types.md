---
ms.openlocfilehash: 088c4a77cecde490c556c44c239a3496f896582e
ms.sourcegitcommit: 4ddf18d000734c1b6d0a48127bf338086fc3f2c3
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 11/05/2019
ms.locfileid: "73616137"
---
# <a name="types"></a><span data-ttu-id="c33ea-101">Types</span><span class="sxs-lookup"><span data-stu-id="c33ea-101">Types</span></span>

<span data-ttu-id="c33ea-102">Typy C# języków są podzielone na dwie główne kategorie: ***typy wartości*** i ***typy odwołań***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-102">The types of the C# language are divided into two main categories: ***value types*** and ***reference types***.</span></span> <span data-ttu-id="c33ea-103">Typy wartości i typy referencyjne mogą być ***typami ogólnymi***, które przyjmują jeden lub więcej ***parametrów typu***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-103">Both value types and reference types may be ***generic types***, which take one or more ***type parameters***.</span></span> <span data-ttu-id="c33ea-104">Parametry typu mogą wyznaczyć zarówno typy wartości, jak i typy odwołań.</span><span class="sxs-lookup"><span data-stu-id="c33ea-104">Type parameters can designate both value types and reference types.</span></span>

```antlr
type
    : value_type
    | reference_type
    | type_parameter
    | type_unsafe
    ;
```

<span data-ttu-id="c33ea-105">Końcowa Kategoria typów, wskaźników jest dostępna tylko w niebezpiecznym kodzie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-105">The final category of types, pointers, is available only in unsafe code.</span></span> <span data-ttu-id="c33ea-106">Omówiono to dokładniej w [typach wskaźników](unsafe-code.md#pointer-types).</span><span class="sxs-lookup"><span data-stu-id="c33ea-106">This is discussed further in [Pointer types](unsafe-code.md#pointer-types).</span></span>

<span data-ttu-id="c33ea-107">Typy wartości różnią się od typów referencyjnych w zmiennych typach wartości bezpośrednio zawierają swoje dane, natomiast zmienne typów referencyjnych przechowują ***odwołania*** do danych, które są znane jako ***obiekty***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-107">Value types differ from reference types in that variables of the value types directly contain their data, whereas variables of the reference types store ***references*** to their data, the latter being known as ***objects***.</span></span> <span data-ttu-id="c33ea-108">W przypadku typów referencyjnych istnieje możliwość, że dwie zmienne odwołują się do tego samego obiektu, i w tym przypadku operacje na jednej zmiennej mają wpływ na obiekt, do którego odwołuje się inna zmienna.</span><span class="sxs-lookup"><span data-stu-id="c33ea-108">With reference types, it is possible for two variables to reference the same object, and thus possible for operations on one variable to affect the object referenced by the other variable.</span></span> <span data-ttu-id="c33ea-109">W przypadku typów wartości zmiennych każda z nich ma własną kopię danych i nie jest możliwe wykonywanie operacji na nich, aby wpływać na inne.</span><span class="sxs-lookup"><span data-stu-id="c33ea-109">With value types, the variables each have their own copy of the data, and it is not possible for operations on one to affect the other.</span></span>

<span data-ttu-id="c33ea-110">C#System typów jest jednorodny tak, że wartość dowolnego typu może być traktowana jako obiekt.</span><span class="sxs-lookup"><span data-stu-id="c33ea-110">C#'s type system is unified such that a value of any type can be treated as an object.</span></span> <span data-ttu-id="c33ea-111">Każdy typ C# bezpośrednio lub pośrednio pochodzi od typu klasy `object`, a `object` jest ostateczną klasą bazową wszystkich typów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-111">Every type in C# directly or indirectly derives from the `object` class type, and `object` is the ultimate base class of all types.</span></span> <span data-ttu-id="c33ea-112">Wartości typów referencyjnych są traktowane jako obiekty po prostu przez wyświetlanie wartości jako typu `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-112">Values of reference types are treated as objects simply by viewing the values as type `object`.</span></span> <span data-ttu-id="c33ea-113">Wartości typów wartości są traktowane jako obiekty przez wykonywanie operacji pakowania i rozpakowywania ([opakowanie i rozpakowywanie](types.md#boxing-and-unboxing)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-113">Values of value types are treated as objects by performing boxing and unboxing operations ([Boxing and unboxing](types.md#boxing-and-unboxing)).</span></span>

## <a name="value-types"></a><span data-ttu-id="c33ea-114">Typy wartości</span><span class="sxs-lookup"><span data-stu-id="c33ea-114">Value types</span></span>

<span data-ttu-id="c33ea-115">Typ wartości jest typem struktury lub typem wyliczenia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-115">A value type is either a struct type or an enumeration type.</span></span> <span data-ttu-id="c33ea-116">C#dostarcza zestaw wstępnie zdefiniowanych typów struktur nazywanych ***typami prostymi***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-116">C# provides a set of predefined struct types called the ***simple types***.</span></span> <span data-ttu-id="c33ea-117">Typy proste są identyfikowane za pomocą słów zarezerwowanych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-117">The simple types are identified through reserved words.</span></span>

```antlr
value_type
    : struct_type
    | enum_type
    ;

struct_type
    : type_name
    | simple_type
    | nullable_type
    ;

simple_type
    : numeric_type
    | 'bool'
    ;

numeric_type
    : integral_type
    | floating_point_type
    | 'decimal'
    ;

integral_type
    : 'sbyte'
    | 'byte'
    | 'short'
    | 'ushort'
    | 'int'
    | 'uint'
    | 'long'
    | 'ulong'
    | 'char'
    ;

floating_point_type
    : 'float'
    | 'double'
    ;

nullable_type
    : non_nullable_value_type '?'
    ;

non_nullable_value_type
    : type
    ;

enum_type
    : type_name
    ;
```

<span data-ttu-id="c33ea-118">W przeciwieństwie do zmiennej typu referencyjnego zmienna typu wartości może zawierać wartość `null` tylko wtedy, gdy typ wartości jest typem dopuszczającym wartość null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-118">Unlike a variable of a reference type, a variable of a value type can contain the value `null` only if the value type is a nullable type.</span></span>  <span data-ttu-id="c33ea-119">Dla każdego typu wartości niedopuszczających wartości null istnieje odpowiadający mu typ wartości null oznaczający ten sam zestaw wartości i wartość `null`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-119">For every non-nullable value type there is a corresponding nullable value type denoting the same set of values plus the value `null`.</span></span>

<span data-ttu-id="c33ea-120">Przypisanie do zmiennej typu wartości tworzy kopię przypisanej wartości.</span><span class="sxs-lookup"><span data-stu-id="c33ea-120">Assignment to a variable of a value type creates a copy of the value being assigned.</span></span> <span data-ttu-id="c33ea-121">Różni się to od przypisywania do zmiennej typu referencyjnego, która kopiuje odwołanie, ale nie obiekt identyfikowany przez odwołanie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-121">This differs from assignment to a variable of a reference type, which copies the reference but not the object identified by the reference.</span></span>

### <a name="the-systemvaluetype-type"></a><span data-ttu-id="c33ea-122">Typ System. ValueType</span><span class="sxs-lookup"><span data-stu-id="c33ea-122">The System.ValueType type</span></span>

<span data-ttu-id="c33ea-123">Wszystkie typy wartości niejawnie dziedziczą z klasy `System.ValueType`, która z kolei dziedziczy z klasy `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-123">All value types implicitly inherit from the class `System.ValueType`, which, in turn, inherits from class `object`.</span></span> <span data-ttu-id="c33ea-124">Nie jest możliwe, aby żaden typ dziedziczył z typu wartości, a typy wartości są w ten sposób niejawnie zapieczętowane ([klasy zapieczętowane](classes.md#sealed-classes)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-124">It is not possible for any type to derive from a value type, and value types are thus implicitly sealed ([Sealed classes](classes.md#sealed-classes)).</span></span>

<span data-ttu-id="c33ea-125">Należy pamiętać, że `System.ValueType` nie należy do *value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-125">Note that `System.ValueType` is not itself a *value_type*.</span></span> <span data-ttu-id="c33ea-126">Zamiast tego jest to *class_type* , z którego wszystkie *value_type*s są wyprowadzane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-126">Rather, it is a *class_type* from which all *value_type*s are automatically derived.</span></span>

### <a name="default-constructors"></a><span data-ttu-id="c33ea-127">Konstruktory domyślne</span><span class="sxs-lookup"><span data-stu-id="c33ea-127">Default constructors</span></span>

<span data-ttu-id="c33ea-128">Wszystkie typy wartości niejawnie deklarują publiczny Konstruktor wystąpienia bez parametrów o nazwie ***Konstruktor domyślny***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-128">All value types implicitly declare a public parameterless instance constructor called the ***default constructor***.</span></span> <span data-ttu-id="c33ea-129">Konstruktor domyślny zwraca wystąpienie inicjowane przez zero, znane jako ***wartość domyślna*** dla typu wartości:</span><span class="sxs-lookup"><span data-stu-id="c33ea-129">The default constructor returns a zero-initialized instance known as the ***default value*** for the value type:</span></span>

*  <span data-ttu-id="c33ea-130">Dla wszystkich *Simple_Type*s wartość domyślna to wartość wytworzony przez wzorzec bitowy dla wszystkich zer:</span><span class="sxs-lookup"><span data-stu-id="c33ea-130">For all *simple_type*s, the default value is the value produced by a bit pattern of all zeros:</span></span>
    * <span data-ttu-id="c33ea-131">W przypadku `sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`i `ulong`wartość domyślna to `0`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-131">For `sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, and `ulong`, the default value is `0`.</span></span>
    * <span data-ttu-id="c33ea-132">W przypadku `char`wartość domyślna to `'\x0000'`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-132">For `char`, the default value is `'\x0000'`.</span></span>
    * <span data-ttu-id="c33ea-133">W przypadku `float`wartość domyślna to `0.0f`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-133">For `float`, the default value is `0.0f`.</span></span>
    * <span data-ttu-id="c33ea-134">W przypadku `double`wartość domyślna to `0.0d`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-134">For `double`, the default value is `0.0d`.</span></span>
    * <span data-ttu-id="c33ea-135">W przypadku `decimal`wartość domyślna to `0.0m`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-135">For `decimal`, the default value is `0.0m`.</span></span>
    * <span data-ttu-id="c33ea-136">W przypadku `bool`wartość domyślna to `false`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-136">For `bool`, the default value is `false`.</span></span>
*  <span data-ttu-id="c33ea-137">W przypadku `E`*enum_type* wartość domyślna to `0`, konwertowana na typ `E`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-137">For an *enum_type* `E`, the default value is `0`, converted to the type `E`.</span></span>
*  <span data-ttu-id="c33ea-138">Dla *struct_type*wartość domyślna to wartość utworzona przez ustawienie wszystkich pól typu wartości na wartości domyślne i wszystkie pola typu odwołania do `null`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-138">For a *struct_type*, the default value is the value produced by setting all value type fields to their default value and all reference type fields to `null`.</span></span>
*  <span data-ttu-id="c33ea-139">Dla *nullable_type* wartość domyślna to wystąpienie, dla którego właściwość `HasValue` ma wartość false, a właściwość `Value` jest niezdefiniowana.</span><span class="sxs-lookup"><span data-stu-id="c33ea-139">For a *nullable_type* the default value is an instance for which the `HasValue` property is false and the `Value` property is undefined.</span></span> <span data-ttu-id="c33ea-140">Wartość domyślna jest również znana jako ***wartość null*** typu Nullable.</span><span class="sxs-lookup"><span data-stu-id="c33ea-140">The default value is also known as the ***null value*** of the nullable type.</span></span>

<span data-ttu-id="c33ea-141">Podobnie jak każdy inny Konstruktor instancji, Konstruktor domyślny typu wartości jest wywoływany przy użyciu operatora `new`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-141">Like any other instance constructor, the default constructor of a value type is invoked using the `new` operator.</span></span> <span data-ttu-id="c33ea-142">Ze względu na wydajność to wymaganie nie jest przeznaczone do rzeczywistej implementacji wywołania konstruktora.</span><span class="sxs-lookup"><span data-stu-id="c33ea-142">For efficiency reasons, this requirement is not intended to actually have the implementation generate a constructor call.</span></span> <span data-ttu-id="c33ea-143">W poniższym przykładzie zmienne `i` i `j` są inicjowane na wartość zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-143">In the example below, variables `i` and `j` are both initialized to zero.</span></span>

```csharp
class A
{
    void F() {
        int i = 0;
        int j = new int();
    }
}
```

<span data-ttu-id="c33ea-144">Ponieważ każdy typ wartości niejawnie ma publicznego konstruktora wystąpienia bez parametrów, nie jest możliwe, aby typ struktury zawierał jawną deklarację konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-144">Because every value type implicitly has a public parameterless instance constructor, it is not possible for a struct type to contain an explicit declaration of a parameterless constructor.</span></span> <span data-ttu-id="c33ea-145">Typ struktury jest jednak dozwolony do deklarowania sparametryzowanych konstruktorów wystąpień ([konstruktorów](structs.md#constructors)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-145">A struct type is however permitted to declare parameterized instance constructors ([Constructors](structs.md#constructors)).</span></span>

### <a name="struct-types"></a><span data-ttu-id="c33ea-146">Typy struktur</span><span class="sxs-lookup"><span data-stu-id="c33ea-146">Struct types</span></span>

<span data-ttu-id="c33ea-147">Typ struktury jest typem wartości, który może deklarować stałe, pola, metody, właściwości, indeksatory, operatory, konstruktory wystąpień, konstruktory statyczne i typy zagnieżdżone.</span><span class="sxs-lookup"><span data-stu-id="c33ea-147">A struct type is a value type that can declare constants, fields, methods, properties, indexers, operators, instance constructors, static constructors, and nested types.</span></span> <span data-ttu-id="c33ea-148">Deklaracja typów struktury jest opisana w [deklaracji struktury](structs.md#struct-declarations).</span><span class="sxs-lookup"><span data-stu-id="c33ea-148">The declaration of struct types is described in [Struct declarations](structs.md#struct-declarations).</span></span>

### <a name="simple-types"></a><span data-ttu-id="c33ea-149">Typy proste</span><span class="sxs-lookup"><span data-stu-id="c33ea-149">Simple types</span></span>

<span data-ttu-id="c33ea-150">C#dostarcza zestaw wstępnie zdefiniowanych typów struktur nazywanych ***typami prostymi***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-150">C# provides a set of predefined struct types called the ***simple types***.</span></span> <span data-ttu-id="c33ea-151">Typy proste są identyfikowane za pomocą słów zarezerwowanych, ale te słowa zastrzeżone są po prostu aliasami dla wstępnie zdefiniowanych typów struktur w przestrzeni nazw `System`, zgodnie z opisem w poniższej tabeli.</span><span class="sxs-lookup"><span data-stu-id="c33ea-151">The simple types are identified through reserved words, but these reserved words are simply aliases for predefined struct types in the `System` namespace, as described in the table below.</span></span>


| <span data-ttu-id="c33ea-152">__Słowo zastrzeżone__</span><span class="sxs-lookup"><span data-stu-id="c33ea-152">__Reserved word__</span></span> | <span data-ttu-id="c33ea-153">__Typ aliasowany__</span><span class="sxs-lookup"><span data-stu-id="c33ea-153">__Aliased type__</span></span> |
|-------------------|------------------|
| `sbyte`           | `System.SByte`   | 
| `byte`            | `System.Byte`    | 
| `short`           | `System.Int16`   | 
| `ushort`          | `System.UInt16`  | 
| `int`             | `System.Int32`   | 
| `uint`            | `System.UInt32`  | 
| `long`            | `System.Int64`   | 
| `ulong`           | `System.UInt64`  | 
| `char`            | `System.Char`    | 
| `float`           | `System.Single`  | 
| `double`          | `System.Double`  | 
| `bool`            | `System.Boolean` | 
| `decimal`         | `System.Decimal` | 

<span data-ttu-id="c33ea-154">Ponieważ typ prosty jest aliasem typu struktury, każdy typ prosty ma składowe.</span><span class="sxs-lookup"><span data-stu-id="c33ea-154">Because a simple type aliases a struct type, every simple type has members.</span></span> <span data-ttu-id="c33ea-155">Na przykład `int` ma członków zadeklarowanych w `System.Int32` i członków dziedziczonych z `System.Object`i są dozwolone następujące instrukcje:</span><span class="sxs-lookup"><span data-stu-id="c33ea-155">For example, `int` has the members declared in `System.Int32` and the members inherited from `System.Object`, and the following statements are permitted:</span></span>

```csharp
int i = int.MaxValue;           // System.Int32.MaxValue constant
string s = i.ToString();        // System.Int32.ToString() instance method
string t = 123.ToString();      // System.Int32.ToString() instance method
```

<span data-ttu-id="c33ea-156">Proste typy różnią się od innych typów struktur w tym, że dopuszczają pewne dodatkowe operacje:</span><span class="sxs-lookup"><span data-stu-id="c33ea-156">The simple types differ from other struct types in that they permit certain additional operations:</span></span>

*  <span data-ttu-id="c33ea-157">Większość typów prostych pozwala na tworzenie wartości, pisząc *literały* ([literały](lexical-structure.md#literals)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-157">Most simple types permit values to be created by writing *literals* ([Literals](lexical-structure.md#literals)).</span></span> <span data-ttu-id="c33ea-158">Na przykład `123` jest literałem typu `int`, a `'a'` jest literałem typu `char`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-158">For example, `123` is a literal of type `int` and `'a'` is a literal of type `char`.</span></span> <span data-ttu-id="c33ea-159">C#nie udostępnia żadnych elementów dla literałów typów struktury ogólnie, a wartości inne niż domyślne innych typów struktury są ostatecznie tworzone przy użyciu konstruktorów wystąpień tych typów struktury.</span><span class="sxs-lookup"><span data-stu-id="c33ea-159">C# makes no provision for literals of struct types in general, and non-default values of other struct types are ultimately always created through instance constructors of those struct types.</span></span>
*  <span data-ttu-id="c33ea-160">Gdy operandy wyrażenia to wszystkie typy proste, możliwe jest, aby kompilator obliczał wyrażenie w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-160">When the operands of an expression are all simple type constants, it is possible for the compiler to evaluate the expression at compile-time.</span></span> <span data-ttu-id="c33ea-161">Takie wyrażenie jest znane jako *constant_expression* ([wyrażenia stałe](expressions.md#constant-expressions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-161">Such an expression is known as a *constant_expression* ([Constant expressions](expressions.md#constant-expressions)).</span></span> <span data-ttu-id="c33ea-162">Wyrażenia obejmujące Operatory zdefiniowane przez inne typy struktur nie są traktowane jako wyrażenia stałe.</span><span class="sxs-lookup"><span data-stu-id="c33ea-162">Expressions involving operators defined by other struct types are not considered to be constant expressions.</span></span>
*  <span data-ttu-id="c33ea-163">Za pomocą deklaracji `const` można zadeklarować stałe typów prostych ([stałych](classes.md#constants)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-163">Through `const` declarations it is possible to declare constants of the simple types ([Constants](classes.md#constants)).</span></span> <span data-ttu-id="c33ea-164">Nie jest możliwe posiadanie stałych innych typów struktur, ale podobny efekt jest dostarczany przez pola `static readonly`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-164">It is not possible to have constants of other struct types, but a similar effect is provided by `static readonly` fields.</span></span>
*  <span data-ttu-id="c33ea-165">Konwersje obejmujące typy proste mogą uczestniczyć w ocenie operatorów konwersji zdefiniowanych przez inne typy struktur, ale Operator konwersji zdefiniowany przez użytkownika nigdy nie uczestniczy w ocenie innego operatora zdefiniowanego przez użytkownika ([Obliczanie Konwersje zdefiniowane przez użytkownika](conversions.md#evaluation-of-user-defined-conversions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-165">Conversions involving simple types can participate in evaluation of conversion operators defined by other struct types, but a user-defined conversion operator can never participate in evaluation of another user-defined operator ([Evaluation of user-defined conversions](conversions.md#evaluation-of-user-defined-conversions)).</span></span>

### <a name="integral-types"></a><span data-ttu-id="c33ea-166">Typy całkowite</span><span class="sxs-lookup"><span data-stu-id="c33ea-166">Integral types</span></span>

<span data-ttu-id="c33ea-167">C#obsługuje dziewięć typów całkowitych: `sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`i `char`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-167">C# supports nine integral types: `sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, and `char`.</span></span> <span data-ttu-id="c33ea-168">Typy całkowite mają następujące rozmiary i zakresy wartości:</span><span class="sxs-lookup"><span data-stu-id="c33ea-168">The integral types have the following sizes and ranges of values:</span></span>

*  <span data-ttu-id="c33ea-169">Typ `sbyte` reprezentuje 8-bitową liczbę całkowitą ze znakiem z wartościami z przedziału od-128 do 127.</span><span class="sxs-lookup"><span data-stu-id="c33ea-169">The `sbyte` type represents signed 8-bit integers with values between -128 and 127.</span></span>
*  <span data-ttu-id="c33ea-170">Typ `byte` reprezentuje 8-bitową liczbę całkowitą bez znaku z wartościami z zakresu od 0 do 255.</span><span class="sxs-lookup"><span data-stu-id="c33ea-170">The `byte` type represents unsigned 8-bit integers with values between 0 and 255.</span></span>
*  <span data-ttu-id="c33ea-171">Typ `short` reprezentuje podpisane 16-bitowe liczby całkowite z wartościami z przedziału od-32768 do 32767.</span><span class="sxs-lookup"><span data-stu-id="c33ea-171">The `short` type represents signed 16-bit integers with values between -32768 and 32767.</span></span>
*  <span data-ttu-id="c33ea-172">Typ `ushort` reprezentuje 16-bitową liczbę całkowitą bez znaku z wartościami z zakresu od 0 do 65535.</span><span class="sxs-lookup"><span data-stu-id="c33ea-172">The `ushort` type represents unsigned 16-bit integers with values between 0 and 65535.</span></span>
*  <span data-ttu-id="c33ea-173">Typ `int` reprezentuje podpisane 32-bitowe liczby całkowite z wartościami z zakresu od – 2147483648 do 2147483647.</span><span class="sxs-lookup"><span data-stu-id="c33ea-173">The `int` type represents signed 32-bit integers with values between -2147483648 and 2147483647.</span></span>
*  <span data-ttu-id="c33ea-174">Typ `uint` reprezentuje niepodpisane 32-bitowe liczby całkowite z wartościami z zakresu od 0 do 4294967295.</span><span class="sxs-lookup"><span data-stu-id="c33ea-174">The `uint` type represents unsigned 32-bit integers with values between 0 and 4294967295.</span></span>
*  <span data-ttu-id="c33ea-175">Typ `long` reprezentuje podpisane 64-bitowe liczby całkowite z wartościami z przedziału od-zakresu od do 9223372036854775807.</span><span class="sxs-lookup"><span data-stu-id="c33ea-175">The `long` type represents signed 64-bit integers with values between -9223372036854775808 and 9223372036854775807.</span></span>
*  <span data-ttu-id="c33ea-176">Typ `ulong` reprezentuje niepodpisane 64-bitowe liczby całkowite z wartościami z zakresu od 0 do 18446744073709551615 są.</span><span class="sxs-lookup"><span data-stu-id="c33ea-176">The `ulong` type represents unsigned 64-bit integers with values between 0 and 18446744073709551615.</span></span>
*  <span data-ttu-id="c33ea-177">Typ `char` reprezentuje 16-bitową liczbę całkowitą bez znaku z wartościami z zakresu od 0 do 65535.</span><span class="sxs-lookup"><span data-stu-id="c33ea-177">The `char` type represents unsigned 16-bit integers with values between 0 and 65535.</span></span> <span data-ttu-id="c33ea-178">Zestaw możliwych wartości dla typu `char` odpowiada zestaw znaków Unicode.</span><span class="sxs-lookup"><span data-stu-id="c33ea-178">The set of possible values for the `char` type corresponds to the Unicode character set.</span></span> <span data-ttu-id="c33ea-179">Chociaż `char` ma tę samą reprezentację co `ushort`, nie wszystkie operacje dozwolone na jednym typie są dozwolone na drugim.</span><span class="sxs-lookup"><span data-stu-id="c33ea-179">Although `char` has the same representation as `ushort`, not all operations permitted on one type are permitted on the other.</span></span>

<span data-ttu-id="c33ea-180">Operatory jednoargumentowe i binarne typu całkowitego są zawsze używane z podpisaną dokładnością 32-bitową, bez znaku 64 64 32</span><span class="sxs-lookup"><span data-stu-id="c33ea-180">The integral-type unary and binary operators always operate with signed 32-bit precision, unsigned 32-bit precision, signed 64-bit precision, or unsigned 64-bit precision:</span></span>

*  <span data-ttu-id="c33ea-181">Dla operatory jednoargumentowe `+` i `~` operand jest konwertowany na typ `T`, gdzie `T` jest pierwszym z `int`, `uint`, `long`i `ulong`, które mogą w pełni reprezentować wszystkie możliwe wartości operandu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-181">For the unary `+` and `~` operators, the operand is converted to type `T`, where `T` is the first of `int`, `uint`, `long`, and `ulong` that can fully represent all possible values of the operand.</span></span> <span data-ttu-id="c33ea-182">Operacja jest następnie wykonywana przy użyciu dokładności typu `T`, a typ wyniku jest `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-182">The operation is then performed using the precision of type `T`, and the type of the result is `T`.</span></span>
*  <span data-ttu-id="c33ea-183">Dla operatora jednoargumentowego `-` operand jest konwertowany na typ `T`, gdzie `T` jest pierwszym z `int` i `long`, które mogą w pełni reprezentować wszystkie możliwe wartości operandu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-183">For the unary `-` operator, the operand is converted to type `T`, where `T` is the first of `int` and `long` that can fully represent all possible values of the operand.</span></span> <span data-ttu-id="c33ea-184">Operacja jest następnie wykonywana przy użyciu dokładności typu `T`, a typ wyniku jest `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-184">The operation is then performed using the precision of type `T`, and the type of the result is `T`.</span></span> <span data-ttu-id="c33ea-185">Jednoargumentowego operatora `-` nie można zastosować do operandów typu `ulong`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-185">The unary `-` operator cannot be applied to operands of type `ulong`.</span></span>
*  <span data-ttu-id="c33ea-186">Dla `+`binarnych, `-`, `*`, `/`, `%`, `&`, `^`, `|`, `==`, `!=`, `>`, `<`, `>=`i `<=` operatorów operandy są konwertowane na typ `T`, gdzie `T` jest pierwszym z `int`, `uint`, `long`i `ulong`, które mogą w pełni reprezentować wszystkie możliwe wartości obu operandów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-186">For the binary `+`, `-`, `*`, `/`, `%`, `&`, `^`, `|`, `==`, `!=`, `>`, `<`, `>=`, and `<=` operators, the operands are converted to type `T`, where `T` is the first of `int`, `uint`, `long`, and `ulong` that can fully represent all possible values of both operands.</span></span> <span data-ttu-id="c33ea-187">Operacja jest następnie wykonywana przy użyciu dokładności typu `T`, a typ wyniku jest `T` (lub `bool` dla operatorów relacyjnych).</span><span class="sxs-lookup"><span data-stu-id="c33ea-187">The operation is then performed using the precision of type `T`, and the type of the result is `T` (or `bool` for the relational operators).</span></span> <span data-ttu-id="c33ea-188">Dla jednego operandu nie można używać typu `long`, a drugi jako typ `ulong` z operatorami dwuargumentowymi.</span><span class="sxs-lookup"><span data-stu-id="c33ea-188">It is not permitted for one operand to be of type `long` and the other to be of type `ulong` with the binary operators.</span></span>
*  <span data-ttu-id="c33ea-189">Dla operatorów `<<` binarnych i `>>`, lewy operand jest konwertowany na typ `T`, gdzie `T` jest pierwszym z `int`, `uint`, `long`i `ulong`, który może w pełni reprezentować wszystkie możliwe wartości operandu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-189">For the binary `<<` and `>>` operators, the left operand is converted to type `T`, where `T` is the first of `int`, `uint`, `long`, and `ulong` that can fully represent all possible values of the operand.</span></span> <span data-ttu-id="c33ea-190">Operacja jest następnie wykonywana przy użyciu dokładności typu `T`, a typ wyniku jest `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-190">The operation is then performed using the precision of type `T`, and the type of the result is `T`.</span></span>

<span data-ttu-id="c33ea-191">Typ `char` jest klasyfikowany jako typ całkowity, ale różni się od innych typów całkowitych na dwa sposoby:</span><span class="sxs-lookup"><span data-stu-id="c33ea-191">The `char` type is classified as an integral type, but it differs from the other integral types in two ways:</span></span>

*  <span data-ttu-id="c33ea-192">Brak niejawnych konwersji z innych typów na typ `char`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-192">There are no implicit conversions from other types to the `char` type.</span></span> <span data-ttu-id="c33ea-193">W szczególności, mimo że typy `sbyte`, `byte`i `ushort` zawierają zakresy wartości, które są w pełni zaprezentowane przy użyciu typu `char`, niejawne konwersje z `sbyte`, `byte`lub `ushort` do `char` nie istnieją.</span><span class="sxs-lookup"><span data-stu-id="c33ea-193">In particular, even though the `sbyte`, `byte`, and `ushort` types have ranges of values that are fully representable using the `char` type, implicit conversions from `sbyte`, `byte`, or `ushort` to `char` do not exist.</span></span>
*  <span data-ttu-id="c33ea-194">Stałe typu `char` muszą być zapisywane jako *character_literal*s lub *integer_literal*s w połączeniu z rzutem na typ `char`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-194">Constants of the `char` type must be written as *character_literal*s or as *integer_literal*s in combination with a cast to type `char`.</span></span> <span data-ttu-id="c33ea-195">Na przykład `(char)10` jest taka sama jak `'\x000A'`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-195">For example, `(char)10` is the same as `'\x000A'`.</span></span>

<span data-ttu-id="c33ea-196">Operatory `checked` i `unchecked` i instrukcje są używane do kontrolowania sprawdzania przepełnienia dla operacji arytmetycznych typu całkowitego i konwersji ([Operatory sprawdzone i niezaznaczone](expressions.md#the-checked-and-unchecked-operators)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-196">The `checked` and `unchecked` operators and statements are used to control overflow checking for integral-type arithmetic operations and conversions ([The checked and unchecked operators](expressions.md#the-checked-and-unchecked-operators)).</span></span> <span data-ttu-id="c33ea-197">W kontekście `checked` przepełnienie powoduje błąd w czasie kompilacji lub powoduje, że `System.OverflowException` zostać zgłoszone.</span><span class="sxs-lookup"><span data-stu-id="c33ea-197">In a `checked` context, an overflow produces a compile-time error or causes a `System.OverflowException` to be thrown.</span></span> <span data-ttu-id="c33ea-198">W kontekście `unchecked` przepełnienia są ignorowane, a wszystkie bity o dużej kolejności, które nie mieszczą się w typie docelowym, są odrzucane.</span><span class="sxs-lookup"><span data-stu-id="c33ea-198">In an `unchecked` context, overflows are ignored and any high-order bits that do not fit in the destination type are discarded.</span></span>

### <a name="floating-point-types"></a><span data-ttu-id="c33ea-199">Typy zmiennoprzecinkowe</span><span class="sxs-lookup"><span data-stu-id="c33ea-199">Floating point types</span></span>

<span data-ttu-id="c33ea-200">C#obsługuje dwa typy zmiennoprzecinkowe: `float` i `double`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-200">C# supports two floating point types: `float` and `double`.</span></span> <span data-ttu-id="c33ea-201">Typy `float` i `double` są reprezentowane przy użyciu 32-bitowych jednoprecyzyjne i 64-bitowe formatów IEEE 754 o podwójnej precyzji, które udostępniają następujące zestawy wartości:</span><span class="sxs-lookup"><span data-stu-id="c33ea-201">The `float` and `double` types are represented using the 32-bit single-precision and 64-bit double-precision IEEE 754 formats, which provide the following sets of values:</span></span>

*  <span data-ttu-id="c33ea-202">Dodatnia zero i ujemna zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-202">Positive zero and negative zero.</span></span> <span data-ttu-id="c33ea-203">W większości sytuacji dodatnie zero i ujemna zero zachowują się identycznie jak wartość zero, ale pewne operacje odróżniają między dwoma ([operatorem dzielenia](expressions.md#division-operator)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-203">In most situations, positive zero and negative zero behave identically as the simple value zero, but certain operations distinguish between the two ([Division operator](expressions.md#division-operator)).</span></span>
*  <span data-ttu-id="c33ea-204">Nieskończoność dodatnia i ujemna nieskończoność.</span><span class="sxs-lookup"><span data-stu-id="c33ea-204">Positive infinity and negative infinity.</span></span> <span data-ttu-id="c33ea-205">Nieskończoności są wytwarzane przez takie operacje jak dzielenie liczby niezerowej przez zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-205">Infinities are produced by such operations as dividing a non-zero number by zero.</span></span> <span data-ttu-id="c33ea-206">Na przykład `1.0 / 0.0` daje nieskończoność dodatnią, a `-1.0 / 0.0` daje ujemną nieskończoność.</span><span class="sxs-lookup"><span data-stu-id="c33ea-206">For example, `1.0 / 0.0` yields positive infinity, and `-1.0 / 0.0` yields negative infinity.</span></span>
*  <span data-ttu-id="c33ea-207">Wartość ***nie-liczbowa*** , często skracany NaN.</span><span class="sxs-lookup"><span data-stu-id="c33ea-207">The ***Not-a-Number*** value, often abbreviated NaN.</span></span> <span data-ttu-id="c33ea-208">NaNs są tworzone przez nieprawidłowe operacje zmiennoprzecinkowe, takie jak dzielenie zera przez zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-208">NaNs are produced by invalid floating-point operations, such as dividing zero by zero.</span></span>
*  <span data-ttu-id="c33ea-209">Skończoną zbiór niezerowych wartości formularza `s * m * 2^e`, gdzie `s` ma wartość 1 lub-1, a `m` i `e` są określane przez określony typ zmiennoprzecinkowy: dla `float`, `0 < m < 2^24` i `-149 <= e <= 104`i dla `double`, `0 < m < 2^53` i `-1075 <= e <= 970`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-209">The finite set of non-zero values of the form `s * m * 2^e`, where `s` is 1 or -1, and `m` and `e` are determined by the particular floating-point type: For `float`, `0 < m < 2^24` and `-149 <= e <= 104`, and for `double`, `0 < m < 2^53` and `-1075 <= e <= 970`.</span></span> <span data-ttu-id="c33ea-210">Nieznormalizowane liczby zmiennoprzecinkowe są uznawane za prawidłowe wartości niezerowe.</span><span class="sxs-lookup"><span data-stu-id="c33ea-210">Denormalized floating-point numbers are considered valid non-zero values.</span></span>

<span data-ttu-id="c33ea-211">Typ `float` może reprezentować wartości z zakresu od około `1.5 * 10^-45` do `3.4 * 10^38` z dokładnością do 7 cyfr.</span><span class="sxs-lookup"><span data-stu-id="c33ea-211">The `float` type can represent values ranging from approximately `1.5 * 10^-45` to `3.4 * 10^38` with a precision of 7 digits.</span></span>

<span data-ttu-id="c33ea-212">Typ `double` może reprezentować wartości z zakresu od około `5.0 * 10^-324` do `1.7 × 10^308` z dokładnością do 15-16 cyfr.</span><span class="sxs-lookup"><span data-stu-id="c33ea-212">The `double` type can represent values ranging from approximately `5.0 * 10^-324` to `1.7 × 10^308` with a precision of 15-16 digits.</span></span>

<span data-ttu-id="c33ea-213">Jeśli jeden z argumentów operatora binarnego jest typu zmiennoprzecinkowego, drugi operand musi być typu całkowitego lub typu zmiennoprzecinkowego, a operacja jest szacowana w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="c33ea-213">If one of the operands of a binary operator is of a floating-point type, then the other operand must be of an integral type or a floating-point type, and the operation is evaluated as follows:</span></span>

*  <span data-ttu-id="c33ea-214">Jeśli jeden z operandów jest typu całkowitego, ten operand jest konwertowany na typ zmiennoprzecinkowy drugiego operandu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-214">If one of the operands is of an integral type, then that operand is converted to the floating-point type of the other operand.</span></span>
*  <span data-ttu-id="c33ea-215">Następnie, jeśli jeden z operandów jest typu `double`, drugi operand jest konwertowany na `double`, operacja jest wykonywana przy użyciu co najmniej `double` zakresu i precyzji, a typ wyniku jest `double` (lub `bool` dla operatorów relacyjnych).</span><span class="sxs-lookup"><span data-stu-id="c33ea-215">Then, if either of the operands is of type `double`, the other operand is converted to `double`, the operation is performed using at least `double` range and precision, and the type of the result is `double` (or `bool` for the relational operators).</span></span>
*  <span data-ttu-id="c33ea-216">W przeciwnym razie operacja jest wykonywana przy użyciu co najmniej `float` zakresu i precyzji, a typ wyniku jest `float` (lub `bool` dla operatorów relacyjnych).</span><span class="sxs-lookup"><span data-stu-id="c33ea-216">Otherwise, the operation is performed using at least `float` range and precision, and the type of the result is `float` (or `bool` for the relational operators).</span></span>

<span data-ttu-id="c33ea-217">Operatory zmiennoprzecinkowe, w tym operatory przypisania, nigdy nie generują wyjątków.</span><span class="sxs-lookup"><span data-stu-id="c33ea-217">The floating-point operators, including the assignment operators, never produce exceptions.</span></span> <span data-ttu-id="c33ea-218">Zamiast tego w wyjątkowych sytuacjach operacje zmiennoprzecinkowe generują zero, nieskończoność lub NaN, zgodnie z poniższym opisem:</span><span class="sxs-lookup"><span data-stu-id="c33ea-218">Instead, in exceptional situations, floating-point operations produce zero, infinity, or NaN, as described below:</span></span>

*  <span data-ttu-id="c33ea-219">Jeśli wynik operacji zmiennoprzecinkowej jest za mały dla formatu docelowego, wynik operacji zmieni się na dodatnią zero lub ujemną zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-219">If the result of a floating-point operation is too small for the destination format, the result of the operation becomes positive zero or negative zero.</span></span>
*  <span data-ttu-id="c33ea-220">Jeśli wynik operacji zmiennoprzecinkowej jest za duży dla formatu docelowego, wynik operacji będzie dodatnią nieskończoność lub ujemną nieskończoność.</span><span class="sxs-lookup"><span data-stu-id="c33ea-220">If the result of a floating-point operation is too large for the destination format, the result of the operation becomes positive infinity or negative infinity.</span></span>
*  <span data-ttu-id="c33ea-221">Jeśli operacja zmiennoprzecinkowa jest nieprawidłowa, wynikiem operacji jest NaN.</span><span class="sxs-lookup"><span data-stu-id="c33ea-221">If a floating-point operation is invalid, the result of the operation becomes NaN.</span></span>
*  <span data-ttu-id="c33ea-222">Jeśli jeden lub oba operandy operacji zmiennoprzecinkowej to NaN, wynik operacji jest NaN.</span><span class="sxs-lookup"><span data-stu-id="c33ea-222">If one or both operands of a floating-point operation is NaN, the result of the operation becomes NaN.</span></span>

<span data-ttu-id="c33ea-223">Operacje zmiennoprzecinkowe mogą być wykonywane z większą dokładnością niż typ wyniku operacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-223">Floating-point operations may be performed with higher precision than the result type of the operation.</span></span> <span data-ttu-id="c33ea-224">Na przykład niektóre architektury sprzętu obsługują typ zmiennoprzecinkowy "rozszerzony" lub "Long Double" z większym zakresem i dokładnością niż typ `double` i niejawnie wykonują wszystkie operacje zmiennoprzecinkowe przy użyciu tego typu większej precyzji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-224">For example, some hardware architectures support an "extended" or "long double" floating-point type with greater range and precision than the `double` type, and implicitly perform all floating-point operations using this higher precision type.</span></span> <span data-ttu-id="c33ea-225">W przypadku nadmiernego kosztu wydajności można przeprowadzić takie architektury sprzętu w celu wykonywania operacji zmiennoprzecinkowych o mniejszej precyzji, a nie wymaganie, aby implementacja przepadał zarówno na C# wydajność, jak i dokładność, a także mieć wyższy poziom precyzji do użycia dla wszystkich operacji zmiennoprzecinkowych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-225">Only at excessive cost in performance can such hardware architectures be made to perform floating-point operations with less precision, and rather than require an implementation to forfeit both performance and precision, C# allows a higher precision type to be used for all floating-point operations.</span></span> <span data-ttu-id="c33ea-226">Inne niż dostarczanie bardziej precyzyjnych wyników, rzadko ma wszelkie wymierne efekty.</span><span class="sxs-lookup"><span data-stu-id="c33ea-226">Other than delivering more precise results, this rarely has any measurable effects.</span></span> <span data-ttu-id="c33ea-227">Jednakże w wyrażeniach `x * y / z`, gdzie mnożenie generuje wynik, który znajduje się poza zakresem `double`, ale kolejny podział wprowadza wynik z powrotem do `double`ego zakresu, fakt, że wyrażenie jest oceniane w Format wyższego poziomu może spowodować powstanie skończonego wyniku zamiast nieskończoności.</span><span class="sxs-lookup"><span data-stu-id="c33ea-227">However, in expressions of the form `x * y / z`, where the multiplication produces a result that is outside the `double` range, but the subsequent division brings the temporary result back into the `double` range, the fact that the expression is evaluated in a higher range format may cause a finite result to be produced instead of an infinity.</span></span>

### <a name="the-decimal-type"></a><span data-ttu-id="c33ea-228">Typ dziesiętny</span><span class="sxs-lookup"><span data-stu-id="c33ea-228">The decimal type</span></span>

<span data-ttu-id="c33ea-229">Typ `decimal` jest 128-bitowym typem danych odpowiednim dla obliczeń finansowych i pieniężnych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-229">The `decimal` type is a 128-bit data type suitable for financial and monetary calculations.</span></span> <span data-ttu-id="c33ea-230">Typ `decimal` może reprezentować wartości z zakresu od `1.0 * 10^-28` do około `7.9 * 10^28` z 28-29 cyframi znaczącymi.</span><span class="sxs-lookup"><span data-stu-id="c33ea-230">The `decimal` type can represent values ranging from `1.0 * 10^-28` to approximately `7.9 * 10^28` with 28-29 significant digits.</span></span>

<span data-ttu-id="c33ea-231">Skończoną zbiór wartości typu `decimal` ma postać `(-1)^s * c * 10^-e`, gdzie znak `s` ma wartość 0 lub 1, współczynnik `c` jest podawany przez `0 <= *c* < 2^96`, a `e` skali jest taka, że `0 <= e <= 28`. Typ `decimal` nie obsługuje podpisanych zer, nieskończoności lub NaN.</span><span class="sxs-lookup"><span data-stu-id="c33ea-231">The finite set of values of type `decimal` are of the form `(-1)^s * c * 10^-e`, where the sign `s` is 0 or 1, the coefficient `c` is given by `0 <= *c* < 2^96`, and the scale `e` is such that `0 <= e <= 28`.The `decimal` type does not support signed zeros, infinities, or NaN's.</span></span> <span data-ttu-id="c33ea-232">`decimal` jest reprezentowane jako 96-bitowa liczba całkowita skalowana z potęgą dziesięciu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-232">A `decimal` is represented as a 96-bit integer scaled by a power of ten.</span></span> <span data-ttu-id="c33ea-233">Dla `decimal`s z wartością bezwzględną mniejszą niż `1.0m`, wartość jest dokładna dla 28 miejsca dziesiętnego, ale nie więcej.</span><span class="sxs-lookup"><span data-stu-id="c33ea-233">For `decimal`s with an absolute value less than `1.0m`, the value is exact to the 28th decimal place, but no further.</span></span> <span data-ttu-id="c33ea-234">Dla `decimal`s z wartością bezwzględną większą lub równą `1.0m`, wartość jest dokładna 28 lub 29 cyfr.</span><span class="sxs-lookup"><span data-stu-id="c33ea-234">For `decimal`s with an absolute value greater than or equal to `1.0m`, the value is exact to 28 or 29 digits.</span></span> <span data-ttu-id="c33ea-235">W przeciwieństwie do typów danych `float` i `double`, liczba ułamków dziesiętnych, takich jak 0,1, można dokładnie przedstawić w reprezentacji `decimal`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-235">Contrary to the `float` and `double` data types, decimal fractional numbers such as 0.1 can be represented exactly in the `decimal` representation.</span></span> <span data-ttu-id="c33ea-236">W `float` i `double` reprezentacje takie liczby są często nieskończonymi ułamkami, co sprawia, że te reprezentacje są bardziej podatne na błędy zaokrąglania.</span><span class="sxs-lookup"><span data-stu-id="c33ea-236">In the `float` and `double` representations, such numbers are often infinite fractions, making those representations more prone to round-off errors.</span></span>

<span data-ttu-id="c33ea-237">Jeśli jeden z argumentów operatora binarnego jest typu `decimal`, drugi operand musi być typu całkowitego lub typu `decimal`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-237">If one of the operands of a binary operator is of type `decimal`, then the other operand must be of an integral type or of type `decimal`.</span></span> <span data-ttu-id="c33ea-238">Jeśli jest obecny operand typu całkowitego, jest konwertowany na `decimal` przed wykonaniem operacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-238">If an integral type operand is present, it is converted to `decimal` before the operation is performed.</span></span>

<span data-ttu-id="c33ea-239">Wynikiem operacji na wartościach typu `decimal` jest to, że wynikiem jest obliczenie dokładnego wyniku (zachowanie skali, zgodnie z definicją dla każdego operatora), a następnie zaokrąglenie w celu dopasowania do reprezentacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-239">The result of an operation on values of type `decimal` is that which would result from calculating an exact result (preserving scale, as defined for each operator) and then rounding to fit the representation.</span></span> <span data-ttu-id="c33ea-240">Wyniki są zaokrąglane do najbliższej reprezentacji wartości i, gdy wynik jest równie zbliżony do dwóch wartości, które można reprezentować, do wartości, która ma parzystą liczbę w najmniejszej liczbie cyfr (jest to nazywane "zaokrągleniem przez Bank").</span><span class="sxs-lookup"><span data-stu-id="c33ea-240">Results are rounded to the nearest representable value, and, when a result is equally close to two representable values, to the value that has an even number in the least significant digit position (this is known as "banker's rounding").</span></span> <span data-ttu-id="c33ea-241">Wynik zero zawsze ma znak 0 i skalę równą 0.</span><span class="sxs-lookup"><span data-stu-id="c33ea-241">A zero result always has a sign of 0 and a scale of 0.</span></span>

<span data-ttu-id="c33ea-242">Jeśli operacja arytmetyczna dziesiętna generuje wartość mniejszą lub równą `5 * 10^-29` w wartości bezwzględnej, wynik operacji zmieni się na zero.</span><span class="sxs-lookup"><span data-stu-id="c33ea-242">If a decimal arithmetic operation produces a value less than or equal to `5 * 10^-29` in absolute value, the result of the operation becomes zero.</span></span> <span data-ttu-id="c33ea-243">Jeśli `decimal` operacja arytmetyczna generuje wynik, który jest zbyt duży dla formatu `decimal`, zostanie zgłoszony `System.OverflowException`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-243">If a `decimal` arithmetic operation produces a result that is too large for the `decimal` format, a `System.OverflowException` is thrown.</span></span>

<span data-ttu-id="c33ea-244">Typ `decimal` ma większą precyzję, ale mniejszy zakres niż typy zmiennoprzecinkowe.</span><span class="sxs-lookup"><span data-stu-id="c33ea-244">The `decimal` type has greater precision but smaller range than the floating-point types.</span></span> <span data-ttu-id="c33ea-245">W ten sposób konwersje z typów zmiennoprzecinkowych na `decimal` mogą generować wyjątki przepełnienia, a konwersje z `decimal` do typów zmiennoprzecinkowych mogą spowodować utratę precyzji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-245">Thus, conversions from the floating-point types to `decimal` might produce overflow exceptions, and conversions from `decimal` to the floating-point types might cause loss of precision.</span></span> <span data-ttu-id="c33ea-246">Z tego względu nie istnieją niejawne konwersje między typami zmiennoprzecinkowymi a `decimal`i bez jawnych rzutowania nie można mieszać argumentów operacji zmiennoprzecinkowych i `decimal` w tym samym wyrażeniu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-246">For these reasons, no implicit conversions exist between the floating-point types and `decimal`, and without explicit casts, it is not possible to mix floating-point and `decimal` operands in the same expression.</span></span>

### <a name="the-bool-type"></a><span data-ttu-id="c33ea-247">Typ bool</span><span class="sxs-lookup"><span data-stu-id="c33ea-247">The bool type</span></span>

<span data-ttu-id="c33ea-248">Typ `bool` reprezentuje logiczne wartości logicznych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-248">The `bool` type represents boolean logical quantities.</span></span> <span data-ttu-id="c33ea-249">Możliwe wartości typu `bool` to `true` i `false`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-249">The possible values of type `bool` are `true` and `false`.</span></span>

<span data-ttu-id="c33ea-250">Między `bool` i innymi typami nie istnieją żadne Konwersje standardowe.</span><span class="sxs-lookup"><span data-stu-id="c33ea-250">No standard conversions exist between `bool` and other types.</span></span> <span data-ttu-id="c33ea-251">W szczególności typ `bool` jest odrębny i oddzielany od typów całkowitych i nie można użyć wartości `bool` zamiast wartości całkowitej i na odwrót.</span><span class="sxs-lookup"><span data-stu-id="c33ea-251">In particular, the `bool` type is distinct and separate from the integral types, and a `bool` value cannot be used in place of an integral value, and vice versa.</span></span>

<span data-ttu-id="c33ea-252">W językach C i C++ , wartość zerowa lub zmiennoprzecinkowa albo wskaźnik o wartości null można przekonwertować na wartość logiczną `false`, a wartość typu całkowitego lub zmiennoprzecinkowego lub wskaźnik o wartości innej niż null można przekonwertować na wartość logiczną `true`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-252">In the C and C++ languages, a zero integral or floating-point value, or a null pointer can be converted to the boolean value `false`, and a non-zero integral or floating-point value, or a non-null pointer can be converted to the boolean value `true`.</span></span> <span data-ttu-id="c33ea-253">W C#programie takie konwersje są wykonywane przez jawne porównanie wartości całkowitej lub zmiennoprzecinkowej na zero lub przez jawne porównanie odwołania do obiektu `null`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-253">In C#, such conversions are accomplished by explicitly comparing an integral or floating-point value to zero, or by explicitly comparing an object reference to `null`.</span></span>

### <a name="enumeration-types"></a><span data-ttu-id="c33ea-254">Typów wyliczeniowych</span><span class="sxs-lookup"><span data-stu-id="c33ea-254">Enumeration types</span></span>

<span data-ttu-id="c33ea-255">Typ wyliczeniowy jest typem odrębnym o nazwanych stałych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-255">An enumeration type is a distinct type with named constants.</span></span> <span data-ttu-id="c33ea-256">Każdy typ wyliczeniowy ma typ podstawowy, który musi mieć `byte`, `sbyte`, `short`, `ushort`, `int`, `uint`, `long` lub `ulong`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-256">Every enumeration type has an underlying type, which must be `byte`, `sbyte`, `short`, `ushort`, `int`, `uint`, `long` or `ulong`.</span></span> <span data-ttu-id="c33ea-257">Zestaw wartości typu wyliczenia jest taki sam jak zestaw wartości typu podstawowego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-257">The set of values of the enumeration type is the same as the set of values of the underlying type.</span></span> <span data-ttu-id="c33ea-258">Wartości typu wyliczenia nie są ograniczone do wartości nazwanych stałych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-258">Values of the enumeration type are not restricted to the values of the named constants.</span></span> <span data-ttu-id="c33ea-259">Typy wyliczeniowe są definiowane za poorednictwem deklaracji wyliczenia ([deklaracji wyliczenia](enums.md#enum-declarations)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-259">Enumeration types are defined through enumeration declarations ([Enum declarations](enums.md#enum-declarations)).</span></span>

### <a name="nullable-types"></a><span data-ttu-id="c33ea-260">Typy dopuszczające wartości null</span><span class="sxs-lookup"><span data-stu-id="c33ea-260">Nullable types</span></span>

<span data-ttu-id="c33ea-261">Typ dopuszczający wartość null może reprezentować wszystkie wartości jego ***typu podstawowego*** oraz dodatkową wartość null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-261">A nullable type can represent all values of its ***underlying type*** plus an additional null value.</span></span> <span data-ttu-id="c33ea-262">Typ dopuszczający wartość null jest zapisywana `T?`, gdzie `T` jest typem podstawowym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-262">A nullable type is written `T?`, where `T` is the underlying type.</span></span> <span data-ttu-id="c33ea-263">Ta składnia jest skrótem `System.Nullable<T>`, a dwa formularze mogą być używane zamiennie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-263">This syntax is shorthand for `System.Nullable<T>`, and the two forms can be used interchangeably.</span></span>

<span data-ttu-id="c33ea-264">***Typ wartości niedopuszczający wartości null*** to dowolny typ wartości inny niż `System.Nullable<T>` i jego skrócona `T?` (dla dowolnej `T`) oraz parametr typu, który jest ograniczony do niedopuszczający wartości null (czyli dowolny parametr typu z `struct` ograniczenie).</span><span class="sxs-lookup"><span data-stu-id="c33ea-264">A ***non-nullable value type*** conversely is any value type other than `System.Nullable<T>` and its shorthand `T?` (for any `T`), plus any type parameter that is constrained to be a non-nullable value type (that is, any type parameter with a `struct` constraint).</span></span> <span data-ttu-id="c33ea-265">Typ `System.Nullable<T>` określa ograniczenie typu wartości dla `T` ([ograniczenia parametru typu](classes.md#type-parameter-constraints)), co oznacza, że typ podstawowy typu dopuszczającego wartość null może być dowolnym typem wartości niedopuszczających wartości null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-265">The `System.Nullable<T>` type specifies the value type constraint for `T` ([Type parameter constraints](classes.md#type-parameter-constraints)), which means that the underlying type of a nullable type can be any non-nullable value type.</span></span> <span data-ttu-id="c33ea-266">Typ podstawowy typu dopuszczającego wartość null nie może być typem dopuszczającym wartość null lub typem referencyjnym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-266">The underlying type of a nullable type cannot be a nullable type or a reference type.</span></span> <span data-ttu-id="c33ea-267">Na przykład `int??` i `string?` są nieprawidłowymi typami.</span><span class="sxs-lookup"><span data-stu-id="c33ea-267">For example, `int??` and `string?` are invalid types.</span></span>

<span data-ttu-id="c33ea-268">Wystąpienie typu dopuszczającego wartość null `T?` ma dwie publiczne właściwości tylko do odczytu:</span><span class="sxs-lookup"><span data-stu-id="c33ea-268">An instance of a nullable type `T?` has two public read-only properties:</span></span>

*  <span data-ttu-id="c33ea-269">Właściwość `HasValue` typu `bool`</span><span class="sxs-lookup"><span data-stu-id="c33ea-269">A `HasValue` property of type `bool`</span></span>
*  <span data-ttu-id="c33ea-270">Właściwość `Value` typu `T`</span><span class="sxs-lookup"><span data-stu-id="c33ea-270">A `Value` property of type `T`</span></span>

<span data-ttu-id="c33ea-271">Wystąpienie, dla którego `HasValue` ma wartość true, mówi o wartości innej niż null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-271">An instance for which `HasValue` is true is said to be non-null.</span></span> <span data-ttu-id="c33ea-272">Wystąpienie o wartości innej niż null zawiera znaną wartość, a `Value` zwraca tę wartość.</span><span class="sxs-lookup"><span data-stu-id="c33ea-272">A non-null instance contains a known value and `Value` returns that value.</span></span>

<span data-ttu-id="c33ea-273">Wystąpienie, dla którego `HasValue` ma wartość false, mówi o wartości null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-273">An instance for which `HasValue` is false is said to be null.</span></span> <span data-ttu-id="c33ea-274">Wystąpienie o wartości null ma niezdefiniowaną wartość.</span><span class="sxs-lookup"><span data-stu-id="c33ea-274">A null instance has an undefined value.</span></span> <span data-ttu-id="c33ea-275">Próba odczytania `Value` wystąpienia o wartości null powoduje zgłoszenie `System.InvalidOperationException`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-275">Attempting to read the `Value` of a null instance causes a `System.InvalidOperationException` to be thrown.</span></span> <span data-ttu-id="c33ea-276">Proces uzyskiwania dostępu do właściwości `Value` wystąpienia wartości null jest określany jako ***odpakowanie***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-276">The process of accessing the `Value` property of a nullable instance is referred to as ***unwrapping***.</span></span>

<span data-ttu-id="c33ea-277">Oprócz konstruktora domyślnego każdy typ dopuszczający wartość null `T?` ma konstruktora publicznego, który przyjmuje jeden argument typu `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-277">In addition to the default constructor, every nullable type `T?` has a public constructor that takes a single argument of type `T`.</span></span> <span data-ttu-id="c33ea-278">Nadana wartość `x` typu `T`, wywołanie konstruktora w postaci</span><span class="sxs-lookup"><span data-stu-id="c33ea-278">Given a value `x` of type `T`, a constructor invocation of the form</span></span>

```csharp
new T?(x)
```
<span data-ttu-id="c33ea-279">tworzy wystąpienie `T?` o wartości innej niż null, dla którego `x`właściwości `Value`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-279">creates a non-null instance of `T?` for which the `Value` property is `x`.</span></span> <span data-ttu-id="c33ea-280">Proces tworzenia wystąpienia o typie innym niż null dla danej wartości jest określany jako ***Zawijanie***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-280">The process of creating a non-null instance of a nullable type for a given value is referred to as ***wrapping***.</span></span>

<span data-ttu-id="c33ea-281">Konwersje niejawne są dostępne z literału `null` do `T?` ([konwersje literałów wartości null](conversions.md#null-literal-conversions)) i z `T` do `T?` ([niejawne konwersje dopuszczające wartość null](conversions.md#implicit-nullable-conversions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-281">Implicit conversions are available from the `null` literal to `T?` ([Null literal conversions](conversions.md#null-literal-conversions)) and from `T` to `T?` ([Implicit nullable conversions](conversions.md#implicit-nullable-conversions)).</span></span>

## <a name="reference-types"></a><span data-ttu-id="c33ea-282">Typy odwołań</span><span class="sxs-lookup"><span data-stu-id="c33ea-282">Reference types</span></span>

<span data-ttu-id="c33ea-283">Typ referencyjny to typ klasy, typ interfejsu, typ tablicy lub typ delegata.</span><span class="sxs-lookup"><span data-stu-id="c33ea-283">A reference type is a class type, an interface type, an array type, or a delegate type.</span></span>

```antlr
reference_type
    : class_type
    | interface_type
    | array_type
    | delegate_type
    ;

class_type
    : type_name
    | 'object'
    | 'dynamic'
    | 'string'
    ;

interface_type
    : type_name
    ;

array_type
    : non_array_type rank_specifier+
    ;

non_array_type
    : type
    ;

rank_specifier
    : '[' dim_separator* ']'
    ;

dim_separator
    : ','
    ;

delegate_type
    : type_name
    ;
```

<span data-ttu-id="c33ea-284">Wartość typu odwołania jest odwołaniem do ***wystąpienia*** typu, które jest znane jako ***obiekt***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-284">A reference type value is a reference to an ***instance*** of the type, the latter known as an ***object***.</span></span> <span data-ttu-id="c33ea-285">Wartość specjalna `null` jest zgodna ze wszystkimi typami odwołań i wskazuje brak wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-285">The special value `null` is compatible with all reference types and indicates the absence of an instance.</span></span>

### <a name="class-types"></a><span data-ttu-id="c33ea-286">Typy klas</span><span class="sxs-lookup"><span data-stu-id="c33ea-286">Class types</span></span>

<span data-ttu-id="c33ea-287">Typ klasy definiuje strukturę danych, która zawiera składowe danych (stałe i pola), składowe funkcji (metody, właściwości, zdarzenia, indeksatory, operatory, konstruktory wystąpień, destruktory i konstruktory statyczne) i typy zagnieżdżone.</span><span class="sxs-lookup"><span data-stu-id="c33ea-287">A class type defines a data structure that contains data members (constants and fields), function members (methods, properties, events, indexers, operators, instance constructors, destructors and static constructors), and nested types.</span></span> <span data-ttu-id="c33ea-288">Typy klas obsługują dziedziczenie, mechanizm, za pomocą którego klasy pochodne mogą poszerzać i specjalizację klas bazowych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-288">Class types support inheritance, a mechanism whereby derived classes can extend and specialize base classes.</span></span> <span data-ttu-id="c33ea-289">Wystąpienia typów klas są tworzone przy użyciu *object_creation_expression*s ([wyrażenia tworzenia obiektów](expressions.md#object-creation-expressions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-289">Instances of class types are created using *object_creation_expression*s ([Object creation expressions](expressions.md#object-creation-expressions)).</span></span>

<span data-ttu-id="c33ea-290">Typy klas są opisane w [klasach](classes.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-290">Class types are described in [Classes](classes.md).</span></span>

<span data-ttu-id="c33ea-291">Niektóre wstępnie zdefiniowane typy klas mają specjalne znaczenie w C# języku, zgodnie z opisem w poniższej tabeli.</span><span class="sxs-lookup"><span data-stu-id="c33ea-291">Certain predefined class types have special meaning in the C# language, as described in the table below.</span></span>


| <span data-ttu-id="c33ea-292">__Typ klasy__</span><span class="sxs-lookup"><span data-stu-id="c33ea-292">__Class type__</span></span>     | <span data-ttu-id="c33ea-293">__Opis__</span><span class="sxs-lookup"><span data-stu-id="c33ea-293">__Description__</span></span>                                         |
|--------------------|---------------------------------------------------------|
| `System.Object`    | <span data-ttu-id="c33ea-294">Ostateczną klasę bazową wszystkich innych typów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-294">The ultimate base class of all other types.</span></span> <span data-ttu-id="c33ea-295">Zobacz [Typ obiektu](types.md#the-object-type).</span><span class="sxs-lookup"><span data-stu-id="c33ea-295">See [The object type](types.md#the-object-type).</span></span> | 
| `System.String`    | <span data-ttu-id="c33ea-296">Typ ciągu C# języka.</span><span class="sxs-lookup"><span data-stu-id="c33ea-296">The string type of the C# language.</span></span> <span data-ttu-id="c33ea-297">Zobacz [typ ciągu](types.md#the-string-type).</span><span class="sxs-lookup"><span data-stu-id="c33ea-297">See [The string type](types.md#the-string-type).</span></span>         |
| `System.ValueType` | <span data-ttu-id="c33ea-298">Klasa bazowa wszystkich typów wartości.</span><span class="sxs-lookup"><span data-stu-id="c33ea-298">The base class of all value types.</span></span> <span data-ttu-id="c33ea-299">Zobacz [Typ System. ValueType](types.md#the-systemvaluetype-type).</span><span class="sxs-lookup"><span data-stu-id="c33ea-299">See [The System.ValueType type](types.md#the-systemvaluetype-type).</span></span>          |
| `System.Enum`      | <span data-ttu-id="c33ea-300">Klasa bazowa wszystkich typów wyliczeniowych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-300">The base class of all enum types.</span></span> <span data-ttu-id="c33ea-301">Zobacz [wyliczenia](enums.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-301">See [Enums](enums.md).</span></span>              |
| `System.Array`     | <span data-ttu-id="c33ea-302">Klasa bazowa wszystkich typów tablicowych.</span><span class="sxs-lookup"><span data-stu-id="c33ea-302">The base class of all array types.</span></span> <span data-ttu-id="c33ea-303">Zobacz [tablice](arrays.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-303">See [Arrays](arrays.md).</span></span>             |
| `System.Delegate`  | <span data-ttu-id="c33ea-304">Klasa bazowa wszystkich typów delegatów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-304">The base class of all delegate types.</span></span> <span data-ttu-id="c33ea-305">Zobacz [Delegaty](delegates.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-305">See [Delegates](delegates.md).</span></span>          |
| `System.Exception` | <span data-ttu-id="c33ea-306">Klasa bazowa wszystkich typów wyjątków.</span><span class="sxs-lookup"><span data-stu-id="c33ea-306">The base class of all exception types.</span></span> <span data-ttu-id="c33ea-307">Zobacz [wyjątki](exceptions.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-307">See [Exceptions](exceptions.md).</span></span>         |

### <a name="the-object-type"></a><span data-ttu-id="c33ea-308">Typ obiektu</span><span class="sxs-lookup"><span data-stu-id="c33ea-308">The object type</span></span>

<span data-ttu-id="c33ea-309">Typ klasy `object` jest ostateczną klasą bazową wszystkich innych typów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-309">The `object` class type is the ultimate base class of all other types.</span></span> <span data-ttu-id="c33ea-310">Każdy typ C# bezpośrednio lub pośrednio pochodzi od typu klasy `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-310">Every type in C# directly or indirectly derives from the `object` class type.</span></span>

<span data-ttu-id="c33ea-311">Słowo kluczowe `object` jest po prostu aliasem wstępnie zdefiniowanej klasy `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-311">The keyword `object` is simply an alias for the predefined class `System.Object`.</span></span>

### <a name="the-dynamic-type"></a><span data-ttu-id="c33ea-312">Typ dynamiczny</span><span class="sxs-lookup"><span data-stu-id="c33ea-312">The dynamic type</span></span>

<span data-ttu-id="c33ea-313">Typ `dynamic`, taki jak `object`, może odwoływać się do dowolnego obiektu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-313">The `dynamic` type, like `object`, can reference any object.</span></span> <span data-ttu-id="c33ea-314">Gdy operatory są stosowane do wyrażeń typu `dynamic`, ich rozdzielczości są odroczone do momentu uruchomienia programu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-314">When operators are applied to expressions of type `dynamic`, their resolution is deferred until the program is run.</span></span> <span data-ttu-id="c33ea-315">W takim przypadku, jeśli operatora nie można legalnie zastosować do obiektu, do którego się odwoływano, podczas kompilacji nie podano żadnego błędu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-315">Thus, if the operator cannot legally be applied to the referenced object, no error is given during compilation.</span></span> <span data-ttu-id="c33ea-316">Zamiast tego wyjątek zostanie wygenerowany, gdy rozwiązanie operatora zakończy się niepowodzeniem w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="c33ea-316">Instead an exception will be thrown when resolution of the operator fails at run-time.</span></span>

<span data-ttu-id="c33ea-317">Jego celem jest umożliwienie dynamicznego powiązania, które opisano szczegółowo w temacie [dynamiczne wiązanie](expressions.md#dynamic-binding).</span><span class="sxs-lookup"><span data-stu-id="c33ea-317">Its purpose is to allow dynamic binding, which is described in detail in [Dynamic binding](expressions.md#dynamic-binding).</span></span>

<span data-ttu-id="c33ea-318">`dynamic` jest uważana za identyczny `object` z wyjątkiem sytuacji, gdy są one następujące:</span><span class="sxs-lookup"><span data-stu-id="c33ea-318">`dynamic` is considered identical to `object` except in the following respects:</span></span>

*  <span data-ttu-id="c33ea-319">Operacje na wyrażeniach typu `dynamic` można powiązać dynamicznie ([powiązanie dynamiczne](expressions.md#dynamic-binding)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-319">Operations on expressions of type `dynamic` can be dynamically bound ([Dynamic binding](expressions.md#dynamic-binding)).</span></span>
*  <span data-ttu-id="c33ea-320">Wnioskowanie o typie ([wnioskowanie o typie](expressions.md#type-inference)) będzie preferowane `dynamic` przez `object`, jeśli oba są kandydatami.</span><span class="sxs-lookup"><span data-stu-id="c33ea-320">Type inference ([Type inference](expressions.md#type-inference)) will prefer `dynamic` over `object` if both are candidates.</span></span>

<span data-ttu-id="c33ea-321">Ze względu na tę równoważność są następujące:</span><span class="sxs-lookup"><span data-stu-id="c33ea-321">Because of this equivalence, the following holds:</span></span>

*  <span data-ttu-id="c33ea-322">Istnieje niejawna konwersja tożsamości między `object` i `dynamic`, i między tworzonymi typami, które są takie same podczas wymiany `dynamic` z `object`</span><span class="sxs-lookup"><span data-stu-id="c33ea-322">There is an implicit identity conversion between `object` and `dynamic`, and between constructed types that are the same when replacing `dynamic` with `object`</span></span>
*  <span data-ttu-id="c33ea-323">Konwersje niejawne i jawne do i z `object` są również stosowane do i z `dynamic`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-323">Implicit and explicit conversions to and from `object` also apply to and from `dynamic`.</span></span>
*  <span data-ttu-id="c33ea-324">Sygnatury metod, które są takie same, gdy zastępowanie `dynamic` z `object` są uważane za ten sam podpis</span><span class="sxs-lookup"><span data-stu-id="c33ea-324">Method signatures that are the same when replacing `dynamic` with `object` are considered the same signature</span></span>
*  <span data-ttu-id="c33ea-325">Typ `dynamic` jest odróżnienie od `object` w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="c33ea-325">The type `dynamic` is indistinguishable from `object` at run-time.</span></span>
*  <span data-ttu-id="c33ea-326">Wyrażenie typu `dynamic` jest określane jako ***wyrażenie dynamiczne***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-326">An expression of the type `dynamic` is referred to as a ***dynamic expression***.</span></span>

### <a name="the-string-type"></a><span data-ttu-id="c33ea-327">Typ ciągu</span><span class="sxs-lookup"><span data-stu-id="c33ea-327">The string type</span></span>

<span data-ttu-id="c33ea-328">Typ `string` jest typem klasy zapieczętowanej, która dziedziczy bezpośrednio z `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-328">The `string` type is a sealed class type that inherits directly from `object`.</span></span> <span data-ttu-id="c33ea-329">Wystąpienia klasy `string` reprezentują ciągi znaków w kodzie Unicode.</span><span class="sxs-lookup"><span data-stu-id="c33ea-329">Instances of the `string` class represent Unicode character strings.</span></span>

<span data-ttu-id="c33ea-330">Wartości typu `string` mogą być zapisywane jako literały ciągu ([literały ciągu](lexical-structure.md#string-literals)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-330">Values of the `string` type can be written as string literals ([String literals](lexical-structure.md#string-literals)).</span></span>

<span data-ttu-id="c33ea-331">Słowo kluczowe `string` jest po prostu aliasem wstępnie zdefiniowanej klasy `System.String`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-331">The keyword `string` is simply an alias for the predefined class `System.String`.</span></span>

### <a name="interface-types"></a><span data-ttu-id="c33ea-332">Typy interfejsów</span><span class="sxs-lookup"><span data-stu-id="c33ea-332">Interface types</span></span>

<span data-ttu-id="c33ea-333">Interfejs definiuje kontrakt.</span><span class="sxs-lookup"><span data-stu-id="c33ea-333">An interface defines a contract.</span></span> <span data-ttu-id="c33ea-334">Klasa lub struktura implementująca interfejs musi być zgodna z umową.</span><span class="sxs-lookup"><span data-stu-id="c33ea-334">A class or struct that implements an interface must adhere to its contract.</span></span> <span data-ttu-id="c33ea-335">Interfejs może dziedziczyć z wielu interfejsów podstawowych, a Klasa lub struktura może zaimplementować wiele interfejsów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-335">An interface may inherit from multiple base interfaces, and a class or struct may implement multiple interfaces.</span></span>

<span data-ttu-id="c33ea-336">Typy interfejsów są opisane w [interfejsach](interfaces.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-336">Interface types are described in [Interfaces](interfaces.md).</span></span>

### <a name="array-types"></a><span data-ttu-id="c33ea-337">Typy tablic</span><span class="sxs-lookup"><span data-stu-id="c33ea-337">Array types</span></span>

<span data-ttu-id="c33ea-338">Tablica jest strukturą danych, która zawiera zero lub więcej zmiennych, do których można uzyskać dostęp za pomocą obliczanych indeksów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-338">An array is a data structure that contains zero or more variables which are accessed through computed indices.</span></span> <span data-ttu-id="c33ea-339">Zmienne zawarte w tablicy, nazywane również elementami tablicy, są tego samego typu, a ten typ jest nazywany typem elementu tablicy.</span><span class="sxs-lookup"><span data-stu-id="c33ea-339">The variables contained in an array, also called the elements of the array, are all of the same type, and this type is called the element type of the array.</span></span>

<span data-ttu-id="c33ea-340">Typy tablic są opisane w [tablicach](arrays.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-340">Array types are described in [Arrays](arrays.md).</span></span>

### <a name="delegate-types"></a><span data-ttu-id="c33ea-341">Typy delegatów</span><span class="sxs-lookup"><span data-stu-id="c33ea-341">Delegate types</span></span>

<span data-ttu-id="c33ea-342">Delegat jest strukturą danych, która odwołuje się do jednej lub kilku metod.</span><span class="sxs-lookup"><span data-stu-id="c33ea-342">A delegate is a data structure that refers to one or more methods.</span></span> <span data-ttu-id="c33ea-343">Dla metod instancji odnosi się także do odpowiednich wystąpień obiektu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-343">For instance methods, it also refers to their corresponding object instances.</span></span>

<span data-ttu-id="c33ea-344">Najbliższy odpowiednik delegata w C lub C++ jest wskaźnikiem funkcji, ale natomiast wskaźnik funkcji może odwoływać się tylko do funkcji statycznych, delegat może odwoływać się zarówno do metod statycznych, jak i wystąpień.</span><span class="sxs-lookup"><span data-stu-id="c33ea-344">The closest equivalent of a delegate in C or C++ is a function pointer, but whereas a function pointer can only reference static functions, a delegate can reference both static and instance methods.</span></span> <span data-ttu-id="c33ea-345">W tym drugim przypadku delegat przechowuje nie tylko odwołanie do punktu wejścia metody, ale również odwołanie do wystąpienia obiektu, dla którego ma zostać wywołana metoda.</span><span class="sxs-lookup"><span data-stu-id="c33ea-345">In the latter case, the delegate stores not only a reference to the method's entry point, but also a reference to the object instance on which to invoke the method.</span></span>

<span data-ttu-id="c33ea-346">Typy delegatów są opisane w [delegatach](delegates.md).</span><span class="sxs-lookup"><span data-stu-id="c33ea-346">Delegate types are described in [Delegates](delegates.md).</span></span>

## <a name="boxing-and-unboxing"></a><span data-ttu-id="c33ea-347">Pakowanie i rozpakowywanie</span><span class="sxs-lookup"><span data-stu-id="c33ea-347">Boxing and unboxing</span></span>

<span data-ttu-id="c33ea-348">Pojęcie opakowania i rozpakowywania jest głównym C#systemem typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-348">The concept of boxing and unboxing is central to C#'s type system.</span></span> <span data-ttu-id="c33ea-349">Udostępnia mostek między *value_type*s i *reference_type*s, umożliwiając przekonwertowanie dowolnej wartości *value_type* na i z typu `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-349">It provides a bridge between *value_type*s and *reference_type*s by permitting any value of a *value_type* to be converted to and from type `object`.</span></span> <span data-ttu-id="c33ea-350">Opakowanie i rozpakowywanie umożliwia ujednolicony widok systemu typów, w którym wartość dowolnego typu może ostatecznie być traktowana jako obiekt.</span><span class="sxs-lookup"><span data-stu-id="c33ea-350">Boxing and unboxing enables a unified view of the type system wherein a value of any type can ultimately be treated as an object.</span></span>

### <a name="boxing-conversions"></a><span data-ttu-id="c33ea-351">Konwersje z opakowania</span><span class="sxs-lookup"><span data-stu-id="c33ea-351">Boxing conversions</span></span>

<span data-ttu-id="c33ea-352">Konwersja z opakowania umożliwia niejawną konwersję *value_type* na *reference_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-352">A boxing conversion permits a *value_type* to be implicitly converted to a *reference_type*.</span></span> <span data-ttu-id="c33ea-353">Istnieją następujące konwersje napakowywania:</span><span class="sxs-lookup"><span data-stu-id="c33ea-353">The following boxing conversions exist:</span></span>

*  <span data-ttu-id="c33ea-354">Z dowolnych *value_type* do typu `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-354">From any *value_type* to the type `object`.</span></span>
*  <span data-ttu-id="c33ea-355">Z dowolnych *value_type* do typu `System.ValueType`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-355">From any *value_type* to the type `System.ValueType`.</span></span>
*  <span data-ttu-id="c33ea-356">Z dowolnych *non_nullable_value_type* do dowolnych *INTERFACE_TYPE* wdrożonych przez *value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-356">From any *non_nullable_value_type* to any *interface_type* implemented by the *value_type*.</span></span>
*  <span data-ttu-id="c33ea-357">Z dowolnego *nullable_type* do dowolnego *INTERFACE_TYPE* zaimplementowane przez typ podstawowy *nullable_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-357">From any *nullable_type* to any *interface_type* implemented by the underlying type of the *nullable_type*.</span></span>
*  <span data-ttu-id="c33ea-358">Z dowolnych *enum_type* do typu `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-358">From any *enum_type* to the type `System.Enum`.</span></span>
*  <span data-ttu-id="c33ea-359">Z dowolnego *nullable_type* z podstawowym *enum_typeem* do typu `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-359">From any *nullable_type* with an underlying *enum_type* to the type `System.Enum`.</span></span>
*  <span data-ttu-id="c33ea-360">Należy zauważyć, że niejawna konwersja z parametru typu zostanie wykonana jako konwersja z opakowania, jeśli w czasie wykonywania zostanie zakończona konwersja z typu wartości na typ referencyjny ([niejawne konwersje dotyczące parametrów typu](conversions.md#implicit-conversions-involving-type-parameters)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-360">Note that an implicit conversion from a type parameter will be executed as a boxing conversion if at run-time it ends up converting from a value type to a reference type ([Implicit conversions involving type parameters](conversions.md#implicit-conversions-involving-type-parameters)).</span></span>

<span data-ttu-id="c33ea-361">Opakowanie wartości *non_nullable_value_type* polega na przydzieleniu wystąpienia obiektu i skopiowaniu wartości *non_nullable_value_type* do tego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-361">Boxing a value of a *non_nullable_value_type* consists of allocating an object instance and copying the *non_nullable_value_type* value into that instance.</span></span>

<span data-ttu-id="c33ea-362">Opakowanie wartości elementu *nullable_type* powoduje utworzenie odwołania o wartości null, jeśli jest to wartość `null` (`HasValue` jest `false`) lub wynik rozpakowywania i napakowanie wartości źródłowej w przeciwnym razie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-362">Boxing a value of a *nullable_type* produces a null reference if it is the `null` value (`HasValue` is `false`), or the result of unwrapping and boxing the underlying value otherwise.</span></span>

<span data-ttu-id="c33ea-363">Rzeczywisty proces pakowania wartości *non_nullable_value_type* jest najlepszym wyjaśnieniem przez urojoność istnienia generycznej ***klasy***opakowywania, która zachowuje się tak, jakby została zadeklarowana w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="c33ea-363">The actual process of boxing a value of a *non_nullable_value_type* is best explained by imagining the existence of a generic ***boxing class***, which behaves as if it were declared as follows:</span></span>

```csharp
sealed class Box<T>: System.ValueType
{
    T value;

    public Box(T t) {
        value = t;
    }
}
```

<span data-ttu-id="c33ea-364">Opakowanie wartości `v` typu `T` obejmuje teraz wykonywanie wyrażenia `new Box<T>(v)`i zwracanie wystąpienia wyniku jako wartości typu `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-364">Boxing of a value `v` of type `T` now consists of executing the expression `new Box<T>(v)`, and returning the resulting instance as a value of type `object`.</span></span> <span data-ttu-id="c33ea-365">W ten sposób instrukcje</span><span class="sxs-lookup"><span data-stu-id="c33ea-365">Thus, the statements</span></span>
```csharp
int i = 123;
object box = i;
```
<span data-ttu-id="c33ea-366">koncepcyjnie odpowiada</span><span class="sxs-lookup"><span data-stu-id="c33ea-366">conceptually correspond to</span></span>
```csharp
int i = 123;
object box = new Box<int>(i);
```

<span data-ttu-id="c33ea-367">Klasa opakowywania, taka jak `Box<T>` powyżej, nie istnieje, a dynamiczny typ wartości opakowanej nie jest w rzeczywistości typem klasy.</span><span class="sxs-lookup"><span data-stu-id="c33ea-367">A boxing class like `Box<T>` above doesn't actually exist and the dynamic type of a boxed value isn't actually a class type.</span></span> <span data-ttu-id="c33ea-368">Zamiast tego, opakowana wartość typu `T` ma `T`typu dynamicznego i sprawdzanie typu dynamicznego przy użyciu operatora `is` może po prostu odwoływać się do typu `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-368">Instead, a boxed value of type `T` has the dynamic type `T`, and a dynamic type check using the `is` operator can simply reference type `T`.</span></span> <span data-ttu-id="c33ea-369">Na przykład</span><span class="sxs-lookup"><span data-stu-id="c33ea-369">For example,</span></span>
```csharp
int i = 123;
object box = i;
if (box is int) {
    Console.Write("Box contains an int");
}
```
<span data-ttu-id="c33ea-370">Program będzie wyprowadzał ciąg "`Box contains an int`" w konsoli.</span><span class="sxs-lookup"><span data-stu-id="c33ea-370">will output the string "`Box contains an int`" on the console.</span></span>

<span data-ttu-id="c33ea-371">Konwersja z opakowania oznacza tworzenie kopii wartości w ramce.</span><span class="sxs-lookup"><span data-stu-id="c33ea-371">A boxing conversion implies making a copy of the value being boxed.</span></span> <span data-ttu-id="c33ea-372">Różni się to od konwersji *reference_type* na typ `object`, w którym wartość nadal odwołuje się do tego samego wystąpienia, a po prostu jest traktowana jako mniej pochodny typ `object`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-372">This is different from a conversion of a *reference_type* to type `object`, in which the value continues to reference the same instance and simply is regarded as the less derived type `object`.</span></span> <span data-ttu-id="c33ea-373">Na przykład, dana deklaracja</span><span class="sxs-lookup"><span data-stu-id="c33ea-373">For example, given the declaration</span></span>
```csharp
struct Point
{
    public int x, y;

    public Point(int x, int y) {
        this.x = x;
        this.y = y;
    }
}
```
<span data-ttu-id="c33ea-374">następujące instrukcje</span><span class="sxs-lookup"><span data-stu-id="c33ea-374">the following statements</span></span>
```csharp
Point p = new Point(10, 10);
object box = p;
p.x = 20;
Console.Write(((Point)box).x);
```
<span data-ttu-id="c33ea-375">spowoduje wygenerowanie wartości 10 w konsoli, ponieważ niejawna operacja opakowywania, która występuje w przypisaniu `p` do `box` powoduje skopiowanie wartości `p`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-375">will output the value 10 on the console because the implicit boxing operation that occurs in the assignment of `p` to `box` causes the value of `p` to be copied.</span></span> <span data-ttu-id="c33ea-376">`Point` został zadeklarowany `class` zamiast tego, wartość 20 byłaby wyjściowa, ponieważ `p` i `box` odwołują się do tego samego wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-376">Had `Point` been declared a `class` instead, the value 20 would be output because `p` and `box` would reference the same instance.</span></span>

### <a name="unboxing-conversions"></a><span data-ttu-id="c33ea-377">Odpakowywanie konwersji</span><span class="sxs-lookup"><span data-stu-id="c33ea-377">Unboxing conversions</span></span>

<span data-ttu-id="c33ea-378">Konwersja rozpakowywanie umożliwia jawne przekonwertowanie *reference_type* na *value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-378">An unboxing conversion permits a *reference_type* to be explicitly converted to a *value_type*.</span></span> <span data-ttu-id="c33ea-379">Istnieją następujące konwersje rozpakowywania:</span><span class="sxs-lookup"><span data-stu-id="c33ea-379">The following unboxing conversions exist:</span></span>

*  <span data-ttu-id="c33ea-380">Z typu `object` do dowolnych *value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-380">From the type `object` to any *value_type*.</span></span>
*  <span data-ttu-id="c33ea-381">Z typu `System.ValueType` do dowolnych *value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-381">From the type `System.ValueType` to any *value_type*.</span></span>
*  <span data-ttu-id="c33ea-382">Z dowolnego *interface_typeu* do dowolnego *non_nullable_value_type* , który implementuje *INTERFACE_TYPE*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-382">From any *interface_type* to any *non_nullable_value_type* that implements the *interface_type*.</span></span>
*  <span data-ttu-id="c33ea-383">Z dowolnego *INTERFACE_TYPE* do dowolnego *nullable_type* , którego typ podstawowy implementuje *INTERFACE_TYPE*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-383">From any *interface_type* to any *nullable_type* whose underlying type implements the *interface_type*.</span></span>
*  <span data-ttu-id="c33ea-384">Z typu `System.Enum` do dowolnych *enum_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-384">From the type `System.Enum` to any *enum_type*.</span></span>
*  <span data-ttu-id="c33ea-385">Z typu `System.Enum` do dowolnego *nullable_type* z podstawowym *enum_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-385">From the type `System.Enum` to any *nullable_type* with an underlying *enum_type*.</span></span>
*  <span data-ttu-id="c33ea-386">Należy zauważyć, że jawna konwersja parametru typu będzie wykonywana jako konwersja rozpakowywanie, jeśli w czasie wykonywania zostanie zakończona konwersja z typu referencyjnego na typ wartości ([jawne konwersje dynamiczne](conversions.md#explicit-dynamic-conversions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-386">Note that an explicit conversion to a type parameter will be executed as an unboxing conversion if at run-time it ends up converting from a reference type to a value type ([Explicit dynamic conversions](conversions.md#explicit-dynamic-conversions)).</span></span>

<span data-ttu-id="c33ea-387">Odpakowywanie operacji do *non_nullable_value_type* polega na pierwszym sprawdzeniu, że wystąpienie obiektu jest wartością opakowaną danego *non_nullable_value_type*, a następnie kopiując wartość z wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-387">An unboxing operation to a *non_nullable_value_type* consists of first checking that the object instance is a boxed value of the given *non_nullable_value_type*, and then copying the value out of the instance.</span></span>

<span data-ttu-id="c33ea-388">Odpakowanie do *nullable_type* generuje wartość null *nullable_type* , jeśli argument źródłowy jest `null`lub zawinięty wynik odpakowania wystąpienia obiektu do typu bazowego *nullable_type* w przeciwnym razie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-388">Unboxing to a *nullable_type* produces the null value of the *nullable_type* if the source operand is `null`, or the wrapped result of unboxing the object instance to the underlying type of the *nullable_type* otherwise.</span></span>

<span data-ttu-id="c33ea-389">Odwołujące się do klasy pakowania urojonego opisanej w poprzedniej sekcji, niepakowanie Konwersja obiektu `box` do `T` *value_type* składa się z wykonywania wyrażenia `((Box<T>)box).value`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-389">Referring to the imaginary boxing class described in the previous section, an unboxing conversion of an object `box` to a *value_type* `T` consists of executing the expression `((Box<T>)box).value`.</span></span> <span data-ttu-id="c33ea-390">W ten sposób instrukcje</span><span class="sxs-lookup"><span data-stu-id="c33ea-390">Thus, the statements</span></span>
```csharp
object box = 123;
int i = (int)box;
```
<span data-ttu-id="c33ea-391">koncepcyjnie odpowiada</span><span class="sxs-lookup"><span data-stu-id="c33ea-391">conceptually correspond to</span></span>
```csharp
object box = new Box<int>(123);
int i = ((Box<int>)box).value;
```

<span data-ttu-id="c33ea-392">Aby konwersja rozpakowanego do danego *non_nullable_value_type* się powiodła w czasie wykonywania, wartość operandu źródła musi być odwołaniem do opakowanej wartości tego *non_nullable_value_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-392">For an unboxing conversion to a given *non_nullable_value_type* to succeed at run-time, the value of the source operand must be a reference to a boxed value of that *non_nullable_value_type*.</span></span> <span data-ttu-id="c33ea-393">Jeśli argumentem źródłowym jest `null`, zostanie zgłoszony `System.NullReferenceException`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-393">If the source operand is `null`, a `System.NullReferenceException` is thrown.</span></span> <span data-ttu-id="c33ea-394">Jeśli operand źródłowy jest odwołaniem do niezgodnego obiektu, zostanie zgłoszony `System.InvalidCastException`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-394">If the source operand is a reference to an incompatible object, a `System.InvalidCastException` is thrown.</span></span>

<span data-ttu-id="c33ea-395">W przypadku niepakowanej konwersji do danego *nullable_typeu* , aby pomyślnie w czasie wykonywania, wartość operandu źródła musi być `null` lub odwołanie do wartości opakowanej bazowego *non_nullable_value_type* *nullable_type*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-395">For an unboxing conversion to a given *nullable_type* to succeed at run-time, the value of the source operand must be either `null` or a reference to a boxed value of the underlying *non_nullable_value_type* of the *nullable_type*.</span></span> <span data-ttu-id="c33ea-396">Jeśli operand źródłowy jest odwołaniem do niezgodnego obiektu, zostanie zgłoszony `System.InvalidCastException`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-396">If the source operand is a reference to an incompatible object, a `System.InvalidCastException` is thrown.</span></span>

## <a name="constructed-types"></a><span data-ttu-id="c33ea-397">Typy skonstruowane</span><span class="sxs-lookup"><span data-stu-id="c33ea-397">Constructed types</span></span>

<span data-ttu-id="c33ea-398">Deklaracja typu ogólnego, sama oznacza ***niezwiązany typ ogólny*** , który jest używany jako "plan" do tworzenia wielu różnych typów w drodze zastosowania ***argumentów typu***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-398">A generic type declaration, by itself, denotes an ***unbound generic type*** that is used as a "blueprint" to form many different types, by way of applying ***type arguments***.</span></span> <span data-ttu-id="c33ea-399">Argumenty typu są zapisywane w nawiasach kątowych (`<` i `>`) bezpośrednio po nazwie typu ogólnego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-399">The type arguments are written within angle brackets (`<` and `>`) immediately following the name of the generic type.</span></span> <span data-ttu-id="c33ea-400">Typ, który zawiera co najmniej jeden argument typu, nazywa się ***skonstruowanym typem***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-400">A type that includes at least one type argument is called a ***constructed type***.</span></span> <span data-ttu-id="c33ea-401">Skonstruowany typ może być używany w większości miejsc w języku, w którym może się pojawić nazwa typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-401">A constructed type can be used in most places in the language in which a type name can appear.</span></span> <span data-ttu-id="c33ea-402">Niepowiązanego typu ogólnego można używać tylko w obrębie elementu *typeof_expression* ([operator typeof](expressions.md#the-typeof-operator)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-402">An unbound generic type can only be used within a *typeof_expression* ([The typeof operator](expressions.md#the-typeof-operator)).</span></span>

<span data-ttu-id="c33ea-403">Skonstruowane typy mogą być również używane w wyrażeniach jako proste nazwy ([proste nazwy](expressions.md#simple-names)) lub podczas uzyskiwania dostępu do elementu członkowskiego ([dostęp do elementu członkowskiego](expressions.md#member-access)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-403">Constructed types can also be used in expressions as simple names ([Simple names](expressions.md#simple-names)) or when accessing a member ([Member access](expressions.md#member-access)).</span></span>

<span data-ttu-id="c33ea-404">Podczas szacowania *namespace_or_type_name* są brane pod uwagę tylko typy ogólne z poprawną liczbą parametrów typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-404">When a *namespace_or_type_name* is evaluated, only generic types with the correct number of type parameters are considered.</span></span> <span data-ttu-id="c33ea-405">Z tego względu można użyć tego samego identyfikatora do identyfikacji różnych typów, o ile typy mają różne liczby parametrów typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-405">Thus, it is possible to use the same identifier to identify different types, as long as the types have different numbers of type parameters.</span></span> <span data-ttu-id="c33ea-406">Jest to przydatne w przypadku mieszania klas ogólnych i nieogólnych w tym samym programie:</span><span class="sxs-lookup"><span data-stu-id="c33ea-406">This is useful when mixing generic and non-generic classes in the same program:</span></span>

```csharp
namespace Widgets
{
    class Queue {...}
    class Queue<TElement> {...}
}

namespace MyApplication
{
    using Widgets;

    class X
    {
        Queue q1;            // Non-generic Widgets.Queue
        Queue<int> q2;       // Generic Widgets.Queue
    }
}
```

<span data-ttu-id="c33ea-407">*TYPE_NAME* może identyfikować skonstruowany typ, chociaż nie określa bezpośrednio parametrów typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-407">A *type_name* might identify a constructed type even though it doesn't specify type parameters directly.</span></span> <span data-ttu-id="c33ea-408">Taka sytuacja może wystąpić, gdy typ jest zagnieżdżony w deklaracji klasy ogólnej, a typ wystąpienia deklaracji zawierającej jest niejawnie używany do wyszukiwania nazw ([typy zagnieżdżone w klasach generycznych](classes.md#nested-types-in-generic-classes)):</span><span class="sxs-lookup"><span data-stu-id="c33ea-408">This can occur where a type is nested within a generic class declaration, and the instance type of the containing declaration is implicitly used for name lookup ([Nested types in generic classes](classes.md#nested-types-in-generic-classes)):</span></span>

```csharp
class Outer<T>
{
    public class Inner {...}

    public Inner i;                // Type of i is Outer<T>.Inner
}
```

<span data-ttu-id="c33ea-409">W niebezpiecznym kodzie, skonstruowany typ nie może być używany jako *unmanaged_type* ([typy wskaźników](unsafe-code.md#pointer-types)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-409">In unsafe code, a constructed type cannot be used as an *unmanaged_type* ([Pointer types](unsafe-code.md#pointer-types)).</span></span>

### <a name="type-arguments"></a><span data-ttu-id="c33ea-410">Argumenty typu</span><span class="sxs-lookup"><span data-stu-id="c33ea-410">Type arguments</span></span>

<span data-ttu-id="c33ea-411">Każdy argument na liście argumentów typu jest po prostu *typem*.</span><span class="sxs-lookup"><span data-stu-id="c33ea-411">Each argument in a type argument list is simply a *type*.</span></span>

```antlr
type_argument_list
    : '<' type_arguments '>'
    ;

type_arguments
    : type_argument (',' type_argument)*
    ;

type_argument
    : type
    ;
```

<span data-ttu-id="c33ea-412">W niebezpiecznym kodzie ([niebezpieczny kod](unsafe-code.md)) element *type_argument* nie może być typem wskaźnika.</span><span class="sxs-lookup"><span data-stu-id="c33ea-412">In unsafe code ([Unsafe code](unsafe-code.md)), a *type_argument* may not be a pointer type.</span></span> <span data-ttu-id="c33ea-413">Każdy argument typu musi spełniać wszelkie ograniczenia dotyczące odpowiedniego parametru typu ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-413">Each type argument must satisfy any constraints on the corresponding type parameter ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>

### <a name="open-and-closed-types"></a><span data-ttu-id="c33ea-414">Otwarte i zamknięte typy</span><span class="sxs-lookup"><span data-stu-id="c33ea-414">Open and closed types</span></span>

<span data-ttu-id="c33ea-415">Wszystkie typy mogą być klasyfikowane jako ***typy otwarte*** lub ***zamknięte***.</span><span class="sxs-lookup"><span data-stu-id="c33ea-415">All types can be classified as either ***open types*** or ***closed types***.</span></span> <span data-ttu-id="c33ea-416">Typ otwarty to typ, który obejmuje parametry typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-416">An open type is a type that involves type parameters.</span></span> <span data-ttu-id="c33ea-417">Dokładniej:</span><span class="sxs-lookup"><span data-stu-id="c33ea-417">More specifically:</span></span>

*  <span data-ttu-id="c33ea-418">Parametr typu definiuje typ otwarty.</span><span class="sxs-lookup"><span data-stu-id="c33ea-418">A type parameter defines an open type.</span></span>
*  <span data-ttu-id="c33ea-419">Typ tablicy jest typem otwartym, jeśli i tylko wtedy, gdy jego typ elementu jest typem otwartym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-419">An array type is an open type if and only if its element type is an open type.</span></span>
*  <span data-ttu-id="c33ea-420">Typ skonstruowany jest typem otwartym, jeśli i tylko wtedy, gdy jeden lub więcej argumentów typu jest typem otwartym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-420">A constructed type is an open type if and only if one or more of its type arguments is an open type.</span></span> <span data-ttu-id="c33ea-421">Skonstruowany zagnieżdżony typ jest typem otwartym, jeśli i tylko wtedy, gdy co najmniej jeden z jego argumentów typu lub argumenty typu zawierającego typy zawierające są typem otwartym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-421">A constructed nested type is an open type if and only if one or more of its type arguments or the type arguments of its containing type(s) is an open type.</span></span>

<span data-ttu-id="c33ea-422">Typ zamknięty to typ, który nie jest typem otwartym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-422">A closed type is a type that is not an open type.</span></span>

<span data-ttu-id="c33ea-423">W czasie wykonywania cały kod w deklaracji typu ogólnego jest wykonywany w kontekście zamkniętego konstruowanego typu, który został utworzony przez zastosowanie argumentów typu do deklaracji ogólnej.</span><span class="sxs-lookup"><span data-stu-id="c33ea-423">At run-time, all of the code within a generic type declaration is executed in the context of a closed constructed type that was created by applying type arguments to the generic declaration.</span></span> <span data-ttu-id="c33ea-424">Każdy parametr typu w ramach typu ogólnego jest powiązany z określonym typem czasu wykonywania.</span><span class="sxs-lookup"><span data-stu-id="c33ea-424">Each type parameter within the generic type is bound to a particular run-time type.</span></span> <span data-ttu-id="c33ea-425">Przetwarzanie wszystkich instrukcji i wyrażeń w czasie wykonywania zawsze odbywa się z typami zamkniętymi, a typy otwarte są wykonywane tylko podczas przetwarzania w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-425">The run-time processing of all statements and expressions always occurs with closed types, and open types occur only during compile-time processing.</span></span>

<span data-ttu-id="c33ea-426">Każdy zamknięty typ skonstruowany ma własny zestaw zmiennych statycznych, które nie są udostępniane żadnym innym zamkniętym skonstruowanym typem.</span><span class="sxs-lookup"><span data-stu-id="c33ea-426">Each closed constructed type has its own set of static variables, which are not shared with any other closed constructed types.</span></span> <span data-ttu-id="c33ea-427">Ponieważ otwarty typ nie istnieje w czasie wykonywania, nie istnieją zmienne statyczne skojarzone z typem otwartym.</span><span class="sxs-lookup"><span data-stu-id="c33ea-427">Since an open type does not exist at run-time, there are no static variables associated with an open type.</span></span> <span data-ttu-id="c33ea-428">Dwa zamknięte typy skonstruowane są tego samego typu, jeśli są zbudowane z tego samego niepowiązanego typu ogólnego i odpowiadające im argumenty typu są tego samego typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-428">Two closed constructed types are the same type if they are constructed from the same unbound generic type, and their corresponding type arguments are the same type.</span></span>

### <a name="bound-and-unbound-types"></a><span data-ttu-id="c33ea-429">Powiązane i niepowiązane typy</span><span class="sxs-lookup"><span data-stu-id="c33ea-429">Bound and unbound types</span></span>

<span data-ttu-id="c33ea-430">***Typ niepowiązany*** nie odwołuje się do typu nieogólnego lub niepowiązanego typu ogólnego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-430">The term ***unbound type*** refers to a non-generic type or an unbound generic type.</span></span> <span data-ttu-id="c33ea-431">***Typ powiązany*** z terminem odwołuje się do typu nieogólnego lub typu złożonego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-431">The term ***bound type*** refers to a non-generic type or a constructed type.</span></span>

<span data-ttu-id="c33ea-432">Typ niepowiązany odwołuje się do jednostki zadeklarowanej przez deklarację typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-432">An unbound type refers to the entity declared by a type declaration.</span></span> <span data-ttu-id="c33ea-433">Niezwiązany typ ogólny nie jest samym typem i nie można go używać jako typu zmiennej, argumentu lub wartości zwracanej lub jako typu podstawowego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-433">An unbound generic type is not itself a type, and cannot be used as the type of a variable, argument or return value, or as a base type.</span></span> <span data-ttu-id="c33ea-434">Jedyną konstrukcją, w której można odwołać się do niepowiązanego typu ogólnego, jest wyrażenie `typeof` ([operator typeof](expressions.md#the-typeof-operator)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-434">The only construct in which an unbound generic type can be referenced is the `typeof` expression ([The typeof operator](expressions.md#the-typeof-operator)).</span></span>

### <a name="satisfying-constraints"></a><span data-ttu-id="c33ea-435">Spełnianie ograniczeń</span><span class="sxs-lookup"><span data-stu-id="c33ea-435">Satisfying constraints</span></span>

<span data-ttu-id="c33ea-436">Za każdym razem, gdy jest przywoływany typ skonstruowany lub metoda ogólna, dostarczone argumenty typu są sprawdzane względem ograniczeń parametrów typu zadeklarowanych w typie ogólnym lub metodzie ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-436">Whenever a constructed type or generic method is referenced, the supplied type arguments are checked against the type parameter constraints declared on the generic type or method ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span> <span data-ttu-id="c33ea-437">Dla każdej klauzuli `where` argument typu `A`, który odnosi się do nazwanego parametru typu, jest sprawdzany względem każdego ograniczenia w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="c33ea-437">For each `where` clause, the type argument `A` that corresponds to the named type parameter is checked against each constraint as follows:</span></span>

*  <span data-ttu-id="c33ea-438">Jeśli ograniczenie jest typem klasy, typem interfejsu lub parametrem typu, niech `C` reprezentuje to ograniczenie z podanymi argumentami typu podstawiane dla wszystkich parametrów typu, które pojawiają się w ograniczeniu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-438">If the constraint is a class type, an interface type, or a type parameter, let `C` represent that constraint with the supplied type arguments substituted for any type parameters that appear in the constraint.</span></span> <span data-ttu-id="c33ea-439">Aby spełnić ograniczenie, musi to być przypadek, w którym typ `A` jest konwertowany na typ `C` przez jedną z następujących wartości:</span><span class="sxs-lookup"><span data-stu-id="c33ea-439">To satisfy the constraint, it must be the case that type `A` is convertible to type `C` by one of the following:</span></span>
    * <span data-ttu-id="c33ea-440">Konwersja tożsamości ([konwersja tożsamości](conversions.md#identity-conversion))</span><span class="sxs-lookup"><span data-stu-id="c33ea-440">An identity conversion ([Identity conversion](conversions.md#identity-conversion))</span></span>
    * <span data-ttu-id="c33ea-441">Niejawna konwersja odwołania ([niejawne konwersje odwołań](conversions.md#implicit-reference-conversions))</span><span class="sxs-lookup"><span data-stu-id="c33ea-441">An implicit reference conversion ([Implicit reference conversions](conversions.md#implicit-reference-conversions))</span></span>
    * <span data-ttu-id="c33ea-442">Konwersja z opakowaniem ([konwersje pakujące](conversions.md#boxing-conversions)), pod warunkiem, że typ A jest typem wartości niedopuszczający wartości null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-442">A boxing conversion ([Boxing conversions](conversions.md#boxing-conversions)), provided that type A is a non-nullable value type.</span></span>
    * <span data-ttu-id="c33ea-443">Niejawne odwołanie, opakowanie lub konwersja parametrów typu z parametru typu `A`, aby `C`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-443">An implicit reference, boxing or type parameter conversion from a type parameter `A` to `C`.</span></span>
*  <span data-ttu-id="c33ea-444">Jeśli ograniczenie jest ograniczeniem typu odwołania (`class`), typ `A` musi spełniać jedną z następujących wartości:</span><span class="sxs-lookup"><span data-stu-id="c33ea-444">If the constraint is the reference type constraint (`class`), the type `A` must satisfy one of the following:</span></span>
    * <span data-ttu-id="c33ea-445">`A` to typ interfejsu, typ klasy, typ delegata lub typ tablicy.</span><span class="sxs-lookup"><span data-stu-id="c33ea-445">`A` is an interface type, class type, delegate type or array type.</span></span> <span data-ttu-id="c33ea-446">Należy pamiętać, że `System.ValueType` i `System.Enum` są typami odwołań, które spełniają to ograniczenie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-446">Note that `System.ValueType` and `System.Enum` are reference types that satisfy this constraint.</span></span>
    * <span data-ttu-id="c33ea-447">`A` jest parametrem typu, który jest znany jako typ referencyjny ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-447">`A` is a type parameter that is known to be a reference type ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>
*  <span data-ttu-id="c33ea-448">Jeśli ograniczenie jest ograniczeniem typu wartości (`struct`), typ `A` musi spełniać jedną z następujących czynności:</span><span class="sxs-lookup"><span data-stu-id="c33ea-448">If the constraint is the value type constraint (`struct`), the type `A` must satisfy one of the following:</span></span>
    * <span data-ttu-id="c33ea-449">`A` jest typem struktury lub typem wyliczeniowym, ale nie typem dopuszczającym wartość null.</span><span class="sxs-lookup"><span data-stu-id="c33ea-449">`A` is a struct type or enum type, but not a nullable type.</span></span> <span data-ttu-id="c33ea-450">Należy zauważyć, że `System.ValueType` i `System.Enum` są typami odwołań, które nie spełniają tego ograniczenia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-450">Note that `System.ValueType` and `System.Enum` are reference types that do not satisfy this constraint.</span></span>
    * <span data-ttu-id="c33ea-451">`A` jest parametrem typu mającym ograniczenie typu wartości ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-451">`A` is a type parameter having the value type constraint ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>
*  <span data-ttu-id="c33ea-452">Jeśli ograniczenie jest ograniczeniem konstruktora `new()`, `A` typu nie może być `abstract` i musi mieć publicznego konstruktora bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-452">If the constraint is the constructor constraint `new()`, the type `A` must not be `abstract` and must have a public parameterless constructor.</span></span> <span data-ttu-id="c33ea-453">Jest to spełnione, jeśli jest spełniony jeden z następujących warunków:</span><span class="sxs-lookup"><span data-stu-id="c33ea-453">This is satisfied if one of the following is true:</span></span>
    * <span data-ttu-id="c33ea-454">`A` jest typem wartości, ponieważ wszystkie typy wartości mają publiczny Konstruktor domyślny ([konstruktory domyślne](types.md#default-constructors)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-454">`A` is a value type, since all value types have a public default constructor ([Default constructors](types.md#default-constructors)).</span></span>
    * <span data-ttu-id="c33ea-455">`A` jest parametrem typu mającym ograniczenie konstruktora ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-455">`A` is a type parameter having the constructor constraint ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>
    * <span data-ttu-id="c33ea-456">`A` jest parametrem typu mającym ograniczenie typu wartości ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-456">`A` is a type parameter having the value type constraint ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>
    * <span data-ttu-id="c33ea-457">`A` jest klasą, która nie jest `abstract` i zawiera jawnie zadeklarowany Konstruktor `public` bez parametrów.</span><span class="sxs-lookup"><span data-stu-id="c33ea-457">`A` is a class that is not `abstract` and contains an explicitly declared `public` constructor with no parameters.</span></span>
    * <span data-ttu-id="c33ea-458">`A` nie `abstract` i ma domyślnego konstruktora ([konstruktorów domyślnych](classes.md#default-constructors)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-458">`A` is not `abstract` and has a default constructor ([Default constructors](classes.md#default-constructors)).</span></span>

<span data-ttu-id="c33ea-459">Błąd czasu kompilacji występuje, jeśli co najmniej jedno ograniczenie parametru typu nie jest spełnione przez podaną liczbę argumentów typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-459">A compile-time error occurs if one or more of a type parameter's constraints are not satisfied by the given type arguments.</span></span>

<span data-ttu-id="c33ea-460">Ponieważ parametry typu nie są dziedziczone, ograniczenia nigdy nie są dziedziczone.</span><span class="sxs-lookup"><span data-stu-id="c33ea-460">Since type parameters are not inherited, constraints are never inherited either.</span></span> <span data-ttu-id="c33ea-461">W poniższym przykładzie `D` musi określić ograniczenie dla parametru typu `T`, aby `T` spełniała ograniczenie narzucone przez klasę bazową `B<T>`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-461">In the example below, `D` needs to specify the constraint on its type parameter `T` so that `T` satisfies the constraint imposed by the base class `B<T>`.</span></span> <span data-ttu-id="c33ea-462">W przeciwieństwie do klasy `E` nie trzeba określać ograniczenia, ponieważ `List<T>` implementuje `IEnumerable` dla wszystkich `T`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-462">In contrast, class `E` need not specify a constraint, because `List<T>` implements `IEnumerable` for any `T`.</span></span>

```csharp
class B<T> where T: IEnumerable {...}

class D<T>: B<T> where T: IEnumerable {...}

class E<T>: B<List<T>> {...}
```

## <a name="type-parameters"></a><span data-ttu-id="c33ea-463">Parametry typu</span><span class="sxs-lookup"><span data-stu-id="c33ea-463">Type parameters</span></span>

<span data-ttu-id="c33ea-464">Parametr typu jest identyfikatorem określającym typ wartości lub typ referencyjny, z którym jest powiązany parametr w czasie wykonywania.</span><span class="sxs-lookup"><span data-stu-id="c33ea-464">A type parameter is an identifier designating a value type or reference type that the parameter is bound to at run-time.</span></span>

```antlr
type_parameter
    : identifier
    ;
```

<span data-ttu-id="c33ea-465">Ponieważ parametr typu można utworzyć przy użyciu wielu różnych rzeczywistych argumentów typu, parametry typu mają nieco różne operacje i ograniczenia niż inne typy.</span><span class="sxs-lookup"><span data-stu-id="c33ea-465">Since a type parameter can be instantiated with many different actual type arguments, type parameters have slightly different operations and restrictions than other types.</span></span> <span data-ttu-id="c33ea-466">Należą do nich następujące elementy:</span><span class="sxs-lookup"><span data-stu-id="c33ea-466">These include:</span></span>

*  <span data-ttu-id="c33ea-467">Parametru typu nie można używać bezpośrednio do deklarowania klasy bazowej ([Klasa bazowa](classes.md#base-class)) lub interfejsu ([listy parametrów typu wariantu](interfaces.md#variant-type-parameter-lists)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-467">A type parameter cannot be used directly to declare a base class ([Base class](classes.md#base-class)) or interface ([Variant type parameter lists](interfaces.md#variant-type-parameter-lists)).</span></span>
*  <span data-ttu-id="c33ea-468">Reguły przeszukiwania elementów członkowskich w parametrach typu są zależne od ograniczeń (jeśli istnieją) stosowanych do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-468">The rules for member lookup on type parameters depend on the constraints, if any, applied to the type parameter.</span></span> <span data-ttu-id="c33ea-469">Są one szczegółowe w [przeszukiwaniu elementów członkowskich](expressions.md#member-lookup).</span><span class="sxs-lookup"><span data-stu-id="c33ea-469">They are detailed in [Member lookup](expressions.md#member-lookup).</span></span>
*  <span data-ttu-id="c33ea-470">Dostępne konwersje dla parametru typu są zależne od ograniczeń (jeśli istnieją) stosowanych do parametru typu.</span><span class="sxs-lookup"><span data-stu-id="c33ea-470">The available conversions for a type parameter depend on the constraints, if any, applied to the type parameter.</span></span> <span data-ttu-id="c33ea-471">Są one szczegółowe w przypadku [niejawnych konwersji obejmujących parametry typu](conversions.md#implicit-conversions-involving-type-parameters) i [jawne konwersje dynamiczne](conversions.md#explicit-dynamic-conversions).</span><span class="sxs-lookup"><span data-stu-id="c33ea-471">They are detailed in [Implicit conversions involving type parameters](conversions.md#implicit-conversions-involving-type-parameters) and [Explicit dynamic conversions](conversions.md#explicit-dynamic-conversions).</span></span>
*  <span data-ttu-id="c33ea-472">Literał `null` nie może zostać skonwertowany na typ określony przez parametr typu, z wyjątkiem tego, że parametr typu jest znany jako typ referencyjny ([niejawne konwersje dotyczące parametrów typu](conversions.md#implicit-conversions-involving-type-parameters)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-472">The literal `null` cannot be converted to a type given by a type parameter, except if the type parameter is known to be a reference type ([Implicit conversions involving type parameters](conversions.md#implicit-conversions-involving-type-parameters)).</span></span> <span data-ttu-id="c33ea-473">Jednak zamiast tego można użyć wyrażenia `default` ([wyrażenie wartości domyślnej](expressions.md#default-value-expressions)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-473">However, a `default` expression ([Default value expressions](expressions.md#default-value-expressions)) can be used instead.</span></span> <span data-ttu-id="c33ea-474">Dodatkowo można porównać wartość z typem przyznanym parametrem typu z `null` przy użyciu `==` i `!=` ([Operatory równości typu referencyjnego](expressions.md#reference-type-equality-operators)), chyba że parametr typu ma ograniczenie typu wartości.</span><span class="sxs-lookup"><span data-stu-id="c33ea-474">In addition, a value with a type given by a type parameter can be compared with `null` using `==` and `!=` ([Reference type equality operators](expressions.md#reference-type-equality-operators)) unless the type parameter has the value type constraint.</span></span>
*  <span data-ttu-id="c33ea-475">Wyrażenia `new` ([wyrażenia tworzenia obiektów](expressions.md#object-creation-expressions)) można używać tylko z parametrem typu, jeśli parametr type jest ograniczony przez *constructor_constraint* lub ograniczenie typu wartości ([ograniczenia parametru typu](classes.md#type-parameter-constraints)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-475">A `new` expression ([Object creation expressions](expressions.md#object-creation-expressions)) can only be used with a type parameter if the type parameter is constrained by a *constructor_constraint* or the value type constraint ([Type parameter constraints](classes.md#type-parameter-constraints)).</span></span>
*  <span data-ttu-id="c33ea-476">Parametru typu nie można używać w dowolnym miejscu w atrybucie.</span><span class="sxs-lookup"><span data-stu-id="c33ea-476">A type parameter cannot be used anywhere within an attribute.</span></span>
*  <span data-ttu-id="c33ea-477">Parametru typu nie można użyć w dostępie do elementu członkowskiego ([dostęp do elementu członkowskiego](expressions.md#member-access)) lub nazwy typu ([przestrzeni nazw i typów](basic-concepts.md#namespace-and-type-names)) do identyfikacji statycznej składowej lub typu zagnieżdżonego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-477">A type parameter cannot be used in a member access ([Member access](expressions.md#member-access)) or type name ([Namespace and type names](basic-concepts.md#namespace-and-type-names)) to identify a static member or a nested type.</span></span>
*  <span data-ttu-id="c33ea-478">W niebezpiecznym kodzie parametr typu nie może być używany jako *unmanaged_type* ([typy wskaźników](unsafe-code.md#pointer-types)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-478">In unsafe code, a type parameter cannot be used as an *unmanaged_type* ([Pointer types](unsafe-code.md#pointer-types)).</span></span>

<span data-ttu-id="c33ea-479">Jako typ, parametry typu są czystym konstrukcjami czasu kompilacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-479">As a type, type parameters are purely a compile-time construct.</span></span> <span data-ttu-id="c33ea-480">W czasie wykonywania każdy parametr typu jest powiązany z typem czasu wykonywania, który został określony przez dostarczenie argumentu typu do deklaracji typu ogólnego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-480">At run-time, each type parameter is bound to a run-time type that was specified by supplying a type argument to the generic type declaration.</span></span> <span data-ttu-id="c33ea-481">W takim przypadku typ zmiennej zadeklarowanej za pomocą parametru typu w czasie wykonywania będzie zamkniętym typem skonstruowanym ([otwarte i zamknięte](types.md#open-and-closed-types)).</span><span class="sxs-lookup"><span data-stu-id="c33ea-481">Thus, the type of a variable declared with a type parameter will, at run-time, be a closed constructed type ([Open and closed types](types.md#open-and-closed-types)).</span></span> <span data-ttu-id="c33ea-482">Wykonywanie wszystkich instrukcji i wyrażeń obejmujących parametry typu w czasie wykonywania przy użyciu rzeczywistego typu, który został dostarczony jako argument typu dla tego parametru.</span><span class="sxs-lookup"><span data-stu-id="c33ea-482">The run-time execution of all statements and expressions involving type parameters uses the actual type that was supplied as the type argument for that parameter.</span></span>

## <a name="expression-tree-types"></a><span data-ttu-id="c33ea-483">Typy drzewa wyrażeń</span><span class="sxs-lookup"><span data-stu-id="c33ea-483">Expression tree types</span></span>

<span data-ttu-id="c33ea-484">***Drzewa wyrażeń umożliwiają wyrażenie*** lambda, które mają być reprezentowane jako struktury danych zamiast kodu wykonywalnego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-484">***Expression trees*** permit lambda expressions to be represented as data structures instead of executable code.</span></span> <span data-ttu-id="c33ea-485">Drzewa wyrażeń to wartości ***typów drzewa wyrażeń*** `System.Linq.Expressions.Expression<D>`, gdzie `D` jest dowolnym typem delegata.</span><span class="sxs-lookup"><span data-stu-id="c33ea-485">Expression trees are values of ***expression tree types*** of the form `System.Linq.Expressions.Expression<D>`, where `D` is any delegate type.</span></span> <span data-ttu-id="c33ea-486">W pozostałej części tej specyfikacji będziemy odwoływać się do tych typów przy użyciu skróconej `Expression<D>`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-486">For the remainder of this specification we will refer to these types using the shorthand `Expression<D>`.</span></span>

<span data-ttu-id="c33ea-487">Jeśli konwersja istnieje z wyrażenia lambda do typu delegata `D`, konwersja istnieje również do typu drzewa wyrażenia `Expression<D>`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-487">If a conversion exists from a lambda expression to a delegate type `D`, a conversion also exists to the expression tree type `Expression<D>`.</span></span> <span data-ttu-id="c33ea-488">Natomiast konwersja wyrażenia lambda na typ delegata generuje delegata, który odwołuje się do kodu wykonywalnego wyrażenia lambda, konwersja na typ drzewa wyrażenia tworzy reprezentację drzewa wyrażenia wyrażenia lambda.</span><span class="sxs-lookup"><span data-stu-id="c33ea-488">Whereas the conversion of a lambda expression to a delegate type generates a delegate that references executable code for the lambda expression, conversion to an expression tree type creates an expression tree representation of the lambda expression.</span></span>

<span data-ttu-id="c33ea-489">Drzewa wyrażeń są wydajnymi reprezentacjami danych w pamięci wyrażeń lambda i tworzą strukturę wyrażenia lambda przezroczystego i jawnego.</span><span class="sxs-lookup"><span data-stu-id="c33ea-489">Expression trees are efficient in-memory data representations of lambda expressions and make the structure of the lambda expression transparent and explicit.</span></span>

<span data-ttu-id="c33ea-490">Podobnie jak typ delegata `D`, `Expression<D>` jest określany jako parametry i zwracane typy, które są takie same jak w przypadku `D`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-490">Just like a delegate type `D`, `Expression<D>` is said to have parameter and return types, which are the same as those of `D`.</span></span>

<span data-ttu-id="c33ea-491">Poniższy przykład przedstawia wyrażenie lambda zarówno jako kod wykonywalny, jak i drzewo wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-491">The following example represents a lambda expression both as executable code and as an expression tree.</span></span> <span data-ttu-id="c33ea-492">Ponieważ istnieje konwersja do `Func<int,int>`, konwersja istnieje również do `Expression<Func<int,int>>`:</span><span class="sxs-lookup"><span data-stu-id="c33ea-492">Because a conversion exists to `Func<int,int>`, a conversion also exists to `Expression<Func<int,int>>`:</span></span>

```csharp
Func<int,int> del = x => x + 1;                    // Code

Expression<Func<int,int>> exp = x => x + 1;        // Data
```

<span data-ttu-id="c33ea-493">Następujące przypisania `del` delegata odwołuje się do metody, która zwraca `x + 1`, a drzewo wyrażenia `exp` odwołuje się do struktury danych opisującej `x => x + 1`wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-493">Following these assignments, the delegate `del` references a method that returns `x + 1`, and the expression tree `exp` references a data structure that describes the expression `x => x + 1`.</span></span>

<span data-ttu-id="c33ea-494">Dokładna definicja typu ogólnego `Expression<D>` jak również precyzyjne reguły konstruowania drzewa wyrażeń, gdy wyrażenie lambda jest konwertowane na typ drzewa wyrażenia, są poza zakresem tej specyfikacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-494">The exact definition of the generic type `Expression<D>` as well as the precise rules for constructing an expression tree when a lambda expression is converted to an expression tree type, are both outside the scope of this specification.</span></span>

<span data-ttu-id="c33ea-495">Ważne jest, aby zapewnić jawne:</span><span class="sxs-lookup"><span data-stu-id="c33ea-495">Two things are important to make explicit:</span></span>

*  <span data-ttu-id="c33ea-496">Nie wszystkie wyrażenia lambda mogą być konwertowane na drzewa wyrażeń.</span><span class="sxs-lookup"><span data-stu-id="c33ea-496">Not all lambda expressions can be converted to expression trees.</span></span> <span data-ttu-id="c33ea-497">Na przykład wyrażenia lambda z treścią instrukcji i wyrażenia lambda zawierające wyrażenia przypisania nie mogą być reprezentowane.</span><span class="sxs-lookup"><span data-stu-id="c33ea-497">For instance, lambda expressions with statement bodies, and lambda expressions containing assignment expressions cannot be represented.</span></span> <span data-ttu-id="c33ea-498">W takich przypadkach konwersja nadal istnieje, ale zakończy się niepowodzeniem w czasie kompilacji.</span><span class="sxs-lookup"><span data-stu-id="c33ea-498">In these cases, a conversion still exists, but will fail at compile-time.</span></span> <span data-ttu-id="c33ea-499">Te wyjątki są szczegółowo opisane w [konwersji funkcji anonimowych](conversions.md#anonymous-function-conversions).</span><span class="sxs-lookup"><span data-stu-id="c33ea-499">These exceptions are detailed in [Anonymous function conversions](conversions.md#anonymous-function-conversions).</span></span>
*   <span data-ttu-id="c33ea-500">`Expression<D>` oferuje metodę wystąpienia `Compile`, która tworzy delegata typu `D`:</span><span class="sxs-lookup"><span data-stu-id="c33ea-500">`Expression<D>` offers an instance method `Compile` which produces a delegate of type `D`:</span></span>

    ```csharp
    Func<int,int> del2 = exp.Compile();
    ```

    <span data-ttu-id="c33ea-501">Wywołanie tego delegata powoduje wykonanie kodu reprezentowanego przez drzewo wyrażenia.</span><span class="sxs-lookup"><span data-stu-id="c33ea-501">Invoking this delegate causes the code represented by the expression tree to be executed.</span></span> <span data-ttu-id="c33ea-502">W efekcie, zgodnie z powyższymi definicjami, del i DEL2 są równoważne, a poniższe dwie instrukcje będą miały ten sam skutek:</span><span class="sxs-lookup"><span data-stu-id="c33ea-502">Thus, given the definitions above, del and del2 are equivalent, and the following two statements will have the same effect:</span></span>

    ```csharp
    int i1 = del(1);
    
    int i2 = del2(1);
    ```

    <span data-ttu-id="c33ea-503">Po wykonaniu tego kodu, `i1` i `i2` będą mieć wartość `2`.</span><span class="sxs-lookup"><span data-stu-id="c33ea-503">After executing this code,  `i1` and `i2` will both have the value `2`.</span></span>

